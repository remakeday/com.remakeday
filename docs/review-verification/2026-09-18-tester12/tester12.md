# 휴먼테스트 기록 — 테스터12

- 참가자 ID: 테스터12 = 사용자 본인(시나리오 디렉터)
- 일시: 2026-09-18 19:06 ~ 19:39 KST
- 접속: http://localhost:3500 (프로덕션에 개발 로그인이 안 떠서 로컬로 진행), 개발 계정
- 코드 기준: `7645286`(팀원 대사창 재설계 + 확인 신호 3종 반영본), 백엔드 19:02 재기동본
- 목적: F11 목표 점수대 재측정 + 새 대사창 체감
- attempt_id: `7ac456d8…`(DB `events.session_id`), 이벤트 624건
- 표본 제외: 19:41 이후 같은 계정으로 생성된 판 3~4개는 컨트롤러의 화면 측정 스크립트가 만든 것 — 점수 분석·피드백 어느 쪽에도 포함하지 않는다.

## 점수 분석 (DB `answer_scored`)

| 회차 | 총점 | 원인 | 동기 | 정체 |
|---|---:|---:|---:|---:|
| 1 | 8.8 | 25.0 | 0 | 0 |
| 2 | 8.8 | 25.0 | 0 | 0 |
| 3 | 27.5 | 25.0 | 0 | 62.5 |
| 4 | 27.5 | 25.0 | 0 | 62.5 |
| 5 | 27.5 | 25.0 | 0 | 62.5 |

- 원숭이손: 1회차 수락(① `chaeyeon-honest`).
- 신의 질문: 1회차 4문항 — "채연은 아픈가?"(unknown, 환급) · "은상에게 전염됐는가?"(unknown, 환급) · "준이 말하는 숫자표는 무엇인가?"(unknown, 환급) · "민석은 누구에게 보고를 하는가?"(unknown). 2회차 2문항 — "오늘 방송에서 관리자는 무엇을 말했는가?"(unknown, 환급) · "민석은 오늘 무엇을 했는가?"(supported). **3~5회차 0문항** — 질문 기능을 쓰지 않았다.
- 규칙: 1회차 채연 설명, 2회차 은상 설명, 3회차 은상 소문 출처, 4회차 민석 설명.
- 밤 기록(notes): 306줄, 고유 문장 107개(파편 265/93, 규칙 관찰 35/9).

**해석 한 줄:** 확인 신호(인정 문장·빈 칸 힌트·추천 질문)가 들어간 뒤에도 궤적은 테스터10(8.8→0)·테스터11(14.6)과 같다 — 동기 칸은 끝까지 0, 3회차 이후로는 신의 질문 기능 자체를 쓰지 않는다. F11 목표 점수대 결정의 근거로 쓴다.

## 사용자 피드백 원문

> 관리자 음성이 채팅창이 올라오면서 잘려. 음성은 다 들릴 수 있게 해야될 것 같아. 원숭이손에 의해 채연이 밥을 먹었다고 했는데 저녁에 음식남은 쟁반이 3개인데 다음 원숭이손대사에는 정오에 채연이 배급 쟁반을 다 비웠다고 하는데 뭔가 안맞는 느낌이야. 대화채팅이 움직임에 따라 전체 창 스크롤도 내려가서 대화가 몇번 남았는지 파악하기 힘들어 전체 스크롤과 대화 스크롤은 구분됐으면 좋겠어. 신의 질문에서 글자배열이 매끄럽지 않아. 매끄럽게 바꿔줘. 신의 질문 3번할 수 있는데 이번판 6번할 수 있게 나왔어. 기록보며쓰기에서 겹치는 파편은 통합해줘. 너무 많이 보여. 질문이 3번인데 6번이라 인식한게 기록으로 알 수 없는 질문은 횟수로 쓰지 않는다고 했는데 이 설계가 잘못된거 같아. 질문은 그냥 3번 고정이고, 질문에 답은 횟수가 올라감에 따라 어느정도 비밀을 노출하는게 맞는거 같아. 진짜 진실 오픈은 아니지만 소스정도는 줄 수 있을꺼 같아. 규칙도 아직 너무 어색해. 근데 원숭이손은 한번 걸리면 전체 판에 적용되는거야? 그판이나 아니면 다음판까지만 걸리는거 아니였어? 확인필요 --> 테스트 끝 마지막에 개발데이터가 그냥 글씨만 많아서 먼가 보여지는게 없는거 같아. 수치와 그래프같은게 있으면 좋겠어.

분류: 프론트엔드/UI · 백엔드/AI 설계. F 번호는 원문 문장 순서를 유지한다.

### F1 — 관리자 음성이 채팅창이 올라오면서 잘림

- 장면: 낮 — 관리자 방송 대사가 타이핑되는 도중 다음 줄이 올라올 때
- 요청:
  > 관리자 음성이 채팅창이 올라오면서 잘려. 음성은 다 들릴 수 있게 해야될 것 같아.
- 해석: 방송 음성이 끝나기 전에 다음 줄로 넘어가면 음성이 중간에 끊긴다. 음성은 끝까지 들리게 하고, 그 뒤에 타이핑이 이어져야 한다.
- 원인: `frontend/components/screens/DayScreen.tsx` 96~108행. `currentLine`(타이핑 활성 줄)이 바뀔 때 방송 음성을 다시 트는 effect가 있는데, 줄이 넘어가는 순간 이전 음성의 cleanup으로 `stopVoice()`가 불려 재생 중인 소리가 끊긴다(`stopVoice` 정의는 55행, 실제 정지 호출은 201행 등).
- 조치: 진행 중(Codex) — 음성 재생 동안 타이핑을 멈추고, 음성이 끝나면 타이핑을 이어가는 방식으로 수정 중.

### F2 — 원숭이손 쟁반 서술 모순

- 장면: 밤 — 원숭이손① `chaeyeon-honest` 수락 이후 관찰 문장
- 요청:
  > 원숭이손에 의해 채연이 밥을 먹었다고 했는데 저녁에 음식남은 쟁반이 3개인데 다음 원숭이손대사에는 정오에 채연이 배급 쟁반을 다 비웠다고 하는데 뭔가 안맞는 느낌이야.
- 해석: 5비트(저녁) 정적 서술은 "음식이 남은 쟁반 세 개가 보인다"라고 고정 서술하는데, 원숭이손① 관찰 문장은 "정오에 쟁반을 다 비웠다"고 말한다. 두 문장을 같이 읽으면 채연이 다 먹었다는 전제와 저녁에도 쟁반이 세 개 남아 있다는 서술이 충돌하는 것처럼 읽힌다.
- 원인: `backend/apps/scenarios/scenario_a/adapter.py` 255행 `BeatDTO(n=5, …)`의 `narration="어둑해진 방. 음식이 남은 쟁반 세 개가 보인다."`가 회차 상태와 무관하게 고정 서술이고, 698행 원숭이손① `observation`("채연이 배급 얘기를 한 날, 정오에 쟁반을 다 비웠다. 저녁에 담요를 두른 사람이 하나 늘었다.")이 별개 텍스트로 병렬 노출된다.
- 조치: 반영 — 원숭이손① 5비트 은상 서술(360행 `SceneActionDTO(beat=5, actor="은상", action="담요를 두른다", …)`)에 "은상의 쟁반은 손도 대지 않은 채다."를 덧붙여, 세 쟁반 중 하나가 은상 몫임을 드러냈다(채연이 먹은 날 이상자가 은상으로 바뀐다는 ① 설계와 맞춘다). 회차 상태에 따라 쟁반 수 자체를 계산해서 바꾸는 것은 제출 후 과제로 남긴다. **추가 반영(20:40, 디렉터 결정)** — 부조화의 뿌리는 1비트였다: ①이 정오만 억제해 2일째부터 "어제 다 먹었다더니 아침엔 또 반을 남긴다"가 반복됐다. ① 효과에 1비트 억제를 추가하고, 받는 날엔 1비트가 이미 지나 실행될 수 없으므로 `_wish_fulfilled`가 공개 비트 앞의 억제를 판정에서 뺀다. 검진 담요는 설계(채연은 먹어도 아픔, 이상자가 은상까지 하나 더)라 그대로.

### F3 — 전체 창 스크롤과 대화 스크롤 미분리

- 장면: 낮 — 대화가 진행되며 화면 전체가 함께 내려가는 상황
- 요청:
  > 대화채팅이 움직임에 따라 전체 창 스크롤도 내려가서 대화가 몇번 남았는지 파악하기 힘들어 전체 스크롤과 대화 스크롤은 구분됐으면 좋겠어.
- 원인(실측): 1280x720 · 1440x900 · 390x844 세 해상도 모두에서 문서 높이가 창 높이보다 126px 크다. 전역 `<footer>`(126px)가 낮 화면 루트의 `h-[calc(100dvh-3.5rem)]` 아래에 그대로 붙어 있어, 대화 스크롤이 아니라 페이지 전체가 스크롤된다.
- 조치: 진행 중(Codex) — `/play`에서 전역 푸터를 표시하지 않도록 수정 중.

### F4 — 신의 질문 화면 글자 배열이 매끄럽지 않음

- 장면: 밤 — 신의 질문 화면(`GodScreen.tsx`)
- 요청:
  > 신의 질문에서 글자배열이 매끄럽지 않아. 매끄럽게 바꿔줘.
- 해석: 어느 블록이 문제인지 세부 확인이 필요하다. 판정 배지·질문·답·조언이 `flex-wrap`으로 섞이는 241~263행 부근(가운데 정렬, 배지·첫 줄·조언 블록 순서)이 유력 후보다.
- 조치: 미착수(어느 블록이 매끄럽지 않은지 확인 대기).

### F5 — 신의 질문이 3번인데 6번으로 인식됨 / 환급 설계 재검토

- 장면: 밤 — 신의 질문 사용 횟수
- 요청:
  > 신의 질문 3번할 수 있는데 이번판 6번할 수 있게 나왔어. […] 질문이 3번인데 6번이라 인식한게 기록으로 알 수 없는 질문은 횟수로 쓰지 않는다고 했는데 이 설계가 잘못된거 같아. 질문은 그냥 3번 고정이고, 질문에 답은 횟수가 올라감에 따라 어느정도 비밀을 노출하는게 맞는거 같아. 진짜 진실 오픈은 아니지만 소스정도는 줄 수 있을꺼 같아.
- 원인: 09-18 아침 반영분이 "알 수 없다"로 판정되면 그 질문을 한 밤 상한 3회에서 빼고 환급하는 구조라, 이론상 최대 6번까지 질문할 수 있다(위 점수표 1회차 4문항 중 3건 unknown·환급, 2회차 2문항 중 1건 unknown·환급이 실제 사례).
- 사용자 결정(19:45): 환급을 철회하고 **3회 고정**으로 되돌린다. 대신 같은 밤 2번째·3번째 질문일수록 공개 사다리(advisor ladder)를 한 단계씩 더 열어, 횟수를 쓸수록 답이 조금씩 더 드러나게 한다(진실 전문 공개는 아님).
- 조치: 진행 중(백엔드 서브에이전트).
- 관련: 테스터9 F19, 테스터10 F5, `docs/superpowers/plans/2026-09-18-god-question-improvements.md`.

### F6 — 기록 보며 쓰기에서 겹치는 파편이 너무 많음

- 장면: 밤 — 답안 작성 화면의 밤 기록(notes) 목록
- 요청:
  > 기록보며쓰기에서 겹치는 파편은 통합해줘. 너무 많이 보여.
- 근거: 이번 판 notes 306줄, 고유 문장 107개(파편 265/93, 규칙 관찰 35/9) — 같은 장면이 장면 서술 전체·행동 한 줄·그림 캡션 세 표현으로 겹치고, 정적 파편이 회차마다 다시 쌓인다.
- 코드 위치: `frontend/components/screens/NightScreen.tsx` 46행 부근 `notesByKind`(kind별 `dedupeByText`로만 중복 제거 — 같은 kind 안에서만 걸러지고, 장면 서술·행동 요약·캡션처럼 kind가 다른 중복 표현은 남는다), 백엔드 notes 적재 로직.
- 조치: 미착수(통합 기준 설계 중).

### F7 — 규칙 문장이 아직 어색함

- 장면: 밤 — 규칙(신의 개입) 관련 문장 전반
- 요청:
  > 규칙도 아직 너무 어색해.
- 해석: 구체적으로 어느 문장·화면이 어색한지 예시 확인이 필요하다.
- 조치: 미착수(예시 확인 대기).

### F8 — 원숭이손 규칙 지속 범위 확인

- 장면: 밤 — 원숭이손 규칙 수락 이후 지속 여부
- 요청:
  > 근데 원숭이손은 한번 걸리면 전체 판에 적용되는거야? 그판이나 아니면 다음판까지만 걸리는거 아니였어? 확인필요
- 답: **판 전체에 적용된다.** 기획서 v9.0 §7.3 "규칙은 판 단위로 지속된다. 받은 뒤의 모든 하루에 소원 장면과 반대 사건이…", 플레이 안내 문구 "남은 모든 날에 걸릴 규칙 하나… 받아들인 규칙은 끝까지 풀리지 않는다"와 코드 동작이 모두 일치한다. 설계를 "그 판만" 또는 "다음 판까지만"으로 바꿀지는 사용자 결정 대기.
- 조치: 답변 완료(설계 변경 없음).

### F9 — 결말 개발 데이터가 글씨만 많고 보여지는 게 없음

- 장면: 결말 — 회고(Retrospective) 화면의 개발 데이터(DevDetails) 영역
- 요청:
  > 테스트 끝 마지막에 개발데이터가 그냥 글씨만 많아서 먼가 보여지는게 없는거 같아. 수치와 그래프같은게 있으면 좋겠어.
- 코드 위치: `frontend/components/Retrospective.tsx`(개발 데이터 섹션 — 텍스트 나열 위주).
- 조치: 미착수(오늘/제출 후 반영 여부 결정 대기). 후보: 회차별 점수·칸별 점수 막대 그래프, 신의 질문 판정 분포.

## F11 재측정 결론

- 목표: 확인 신호 3종(인정 문장·빈 칸 힌트·신의 개입 추천 질문) 반영 뒤 점수대가 달라지는지 재확인.
- 결과: 1~2회차 8.8, 3~5회차 27.5로 정체 — 동기 칸은 끝까지 0, 3회차 이후 신의 질문 기능을 아예 쓰지 않았다. 궤적이 테스터10(8.8→0)·테스터11(14.6)과 다르지 않다.
- **목표 점수대는 결정 대기(사용자).** 확인 신호만으로는 궤적이 바뀌지 않는다는 관측만 이번 판으로 확정한다.

- 사용자 보충(2026-09-18 20:20): 3회차 이후 질문을 안 쓴 이유는 기능이 쓸모없어서가 아니라 **환급 때문에 횟수가 안 줄어 "남았다"는 표시가 계속 떠서** 1회차에 4번 묻고 그 뒤로 손을 뗀 것. 따라서 "3회차 이후 0문항"은 F5(환급)의 결과이지 확인 신호(F1·F3·F5 답변 품질)가 안 먹힌 증거가 아니다. F11 판단은 F5(3회 고정)가 들어간 판에서 다시 본다.
