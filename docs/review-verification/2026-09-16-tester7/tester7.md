# 휴먼테스트 기록 — 테스터7

- 참가자 ID: 테스터7 (사용자 지정)
- 상태: 5회차 완주 (clear)
- 일시: 2026-09-16 (F1·F2) · 2026-09-17 10:08 ~ 10:56 KST (완주 판)
- 접속: http://localhost:3500/play
- attempt_id: `61acc8ef-0a57-4b16-9ba3-00bbcda2db4b` (사용자 확인 2026-09-17 · 이벤트 606건 · loop 1~5)
- 인스펙터: http://localhost:3500/inspector/61acc8ef-0a57-4b16-9ba3-00bbcda2db4b
- 이전 후보 `e7aa6a4f-adad-49cb-982d-c600bced1737`(09-16 10:33)은 1회차 첫 장면에서 멈춘 판(이벤트 15건)이라 표본에서 제외
- 기기·화면 크기: 미확인

## 회차별 결과 (DB `nights` 읽기 전용 조회)

| 회차 | 총점 | 통과 | cause | motive | identity | side_effect | 제출문 길이 | 탭한 근거 |
|---|---:|---|---:|---:|---:|---:|---:|---:|
| 1 | 33.5 | X | 75.0 | 20.8 | 0 | 0 | 340자 | 3 |
| 2 | 40.8 | X | 75.0 | 41.7 | 0 | 0 | 383자 | 3 |
| 3 | 66.9 | O | 75.0 | 62.5 | 62.5 | 0 | 622자 | 3 |
| 4 | 66.9 | O | 75.0 | 62.5 | 62.5 | 0 | 622자 | 3 |
| 5 | 94.2 | O | 100 | 83.3 | 100 | 0 | 761자 | 3 |

- side_effect 셀은 끝까지 0 (테스터6과 동일).

## 사용자 피드백 원문

### F1 — 출력 텍스트 선택 (2026-09-16)

- 장면: 낮(Day) — 관리자 방송 텍스트가 드래그로 선택된 상태(스크린샷 첨부)
- 요청:
  > 게임에 출력되는 텍스트가 선택되는 케이스는 게임 컨셉 상 불필요한 요소인 것 같아. 막았으면 좋겠어
- 해석: 게임 화면에 출력되는 내러티브·대사 등 읽기 전용 텍스트의 드래그/복사를 막고, `user-select: none` 등으로 선택 불가 처리.
- 조치: 미착수 (기록만)

### F2 — 장면 미등장 NPC 딤·선택 불가 (2026-09-16)

- 장면: 낮(Day) — NPC 선택 UI
- 요청:
  > 장면이 등장하지 않는 대화 가능 npc는 딤처리 되어서 선택이 불가능해도 좋을 것 같아
- 해석: 현재 장면에 없는 NPC는 목록에서 딤(흐림) 처리하고 탭/선택을 막아, 대화 가능한 인물만 고를 수 있게 한다. (이미 그 비트에 말한 NPC 딤과는 별개 — 장면 부재 기준)
- 추가 해석 (2026-09-18 코드 확인): 현재 `frontend/components/day/DialogueStage.tsx`는 고정 4인 목록을 그대로 쓰고, 장면에 없는 인물은 딤·선택 불가 대신 status가 "인물 확인 중"으로 나온다. 이 라벨은 "장면에 없다"는 실제 상황과 어긋나 요청이 가리키는 문제(딤·선택 불가)를 아직 해결하지 못한다.
- 조치: 미착수 (기록만)

### F3 — NPC 선택 창 여백·2×2 배열 (2026-09-17)

- 장면: 낮(Day) — 대화할 캐릭터 선택 창
- 요청:
  > 대화할 캐릭터를 선택하는 창에 여백이 너무 많이 남음, 2x2 배열로 각 캐릭터 별 UI의 크기를 더 키우는 것이 어떠할까 함
- 해석: NPC 선택 UI를 2×2 격자로 재배치하고, 카드(초상·이름 등) 면적을 키워 남는 여백을 줄인다.
- 조치: 반영 (2026-09-18 저녁, 팀원 프론트 작업) — 낮 화면 재구성 직후에는 이름만 4칸 한 줄이었으나, 같은 날 저녁 대사창을 누적 채팅으로 바꾸면서 인물 초상을 왼쪽 아래에 두고 그 초상 너비에 맞춘 **2×2 카드**로 재배치했다(`frontend/components/day/DialogueStage.tsx`의 `grid-cols-2`, 초상 너비를 `ResizeObserver`로 맞춤). 카드 면적이 커져 원래 요청과 일치.

### F4 — 신의 개입 판정·부연 모순 (2026-09-17)

- 장면: 신의 개입(또는 동등한 질문 답변) — 스크린샷 첨부
- 질문 원문: `채연은 병에 걸린게 확실해 ?`
- 화면 출력: 판정 **「맞다.」**(강조) + 부연 「채연이 계속 담요를 쓰고 있다는 사실과 검진 결과가 공개되지 않았다는 점을 통해 **확신할 수 없다.**」
- 요청:
  > 병에 걸린게 맞다고 하지만 부연 설명에서는 확신할 수 없다고 하는 문제가 있어. 해결이 필요한 부분인 것 같아.
- 해석: 단답 판정(맞다)과 근거·부연의 결론(확신할 수 없다)이 논리적으로 충돌한다. 판정 라벨·부연·근거 추출이 같은 명제를 가리키도록 맞추거나, 불확실하면 「맞다」를 쓰지 않도록 수정이 필요하다.
- 조치: 반영 (2026-09-18, 커밋 `cf5a0b7`) — 테스터9 F25와 같은 건. `advisor_advice.py`의 `_HEDGE_CUES`에 헤지 어휘(예: "확인이 필요", "확실치 않", "확실하지 않" 등)가 부연에 있으면 판정을 "맞다."로 내지 않도록 `settle_status()`가 `unknown`으로 낮춘다. 09-18 스모크에서 "확실치 않"·"확실하지 않"을 목록에 추가했다.

### F5 — 대화 맥락 단절 (2026-09-17)

- 장면: 낮(Day) — NPC 대화 (스크린샷 첨부)
- 관찰 예:
  - 직전/선택 발화: `뭐가 이상해?`
  - 민석 응답: `잘 모르겠어. 방송에서 말한 것처럼 이상한 건 바로 알리라고 했으니까, 만약 뭔가 이상하면 방송실에 알려야 해.`
  - 앞선 맥락으로 보이는 말풍선: `내 얘기도 했어?` / 시스템: `단서 기록에 새 내용을 저장했다.`
- 요청:
  > 대화의 맥락이 이어지지 않는 케이스가 있는 것 같아. 해결이 필요한 부분인 것 같아.
- 해석: 후속 질문이 직전 발화·장면 맥락을 받지 못하고, 일반 규정 안내로 빠져 맥락이 끊긴다. NPC 문맥(직전 턴·당일 기억·질문 지시어) 연결 점검이 필요하다. (F1~F4와 별개의 대화 품질 이슈)
- 조치: 미착수 (기록만)

### F6 — 질문 유형과 답변 의미가 어긋남 (2026-09-17)

- 장면: 신의 개입(또는 동등한 질문 답변) — 스크린샷 첨부
- 질문 원문: `채연은 검진 결과로 무슨 말을 들었나?` (내용·대상 질문)
- 화면 출력: 판정 **「맞다.」** + 부연 「담당자가 채연의 이마를 짚고 지나갔으나 결과는 알려지지 않았다.」 / `▶ 근거 보기 · 2개`
- 요청:
  > 질문에 대한 답변의 의미가 모호한 것 같아. 해결이 필요한 부분인 것 같아
- 해석: 「무슨 말을 들었나」에 예/아니오형 「맞다」가 붙어 답이 무엇을 긍정하는지 모호하다. 질문 유형(여부/내용/원인 등)에 맞는 판정 라벨·답변 형식이 필요하고, F4(맞다 vs 확신할 수 없다)와 같은 신의 개입 답변 일관성 계열로 묶을 수 있다.
- 조치: 미착수 (기록만)

### F7 — 동일 시점 다음 상황 안내 강조 (2026-09-17)

- 장면: 근거/장면 그림 열람 — `이전 그림` · `1 / 2` · `다음 그림` (스크린샷 첨부)
- 요청:
  > 반짝거리는 등의 강조 효과를 통해서 놓치지 않고 동일 시점 내 다음 상황을 빠짐없이 볼 수 있게 해주면 더 좋을 것 같아
- 해석: 같은 시점에 연결된 다음 그림·상황이 남아 있을 때(`n / m`에서 아직 다음이 있을 때) 「다음 그림」 등 진행 단서를 반짝임·펄스 같은 강조로 눈에 띄게 해, 한 장만 보고 닫는 누락을 줄인다.
- 조치: 미착수 (기록만)

### F8 — 상황 이미지를 배경 수준으로 확대 (2026-09-17)

- 장면: 근거/장면 그림 열람·낮 장면 등 상황 이미지 표시 전반
- 요청:
  > 상황에 대한 이미지가 배경이미지 수준으로 크게 보여지게 할 수 있으면 더 좋을 것 같아. 어떻게 적용할 수 있을지는 같이 고민해보면 좋을 것 같고
- 해석: 현재 카드·갤러리 안의 인셋 이미지를, 진입/아침처럼 화면을 채우는 배경급으로 키우고 싶다. 적용 방식은 미정 — 아래 선택지를 플레이 체감과 함께 정할 것.
- 적용 후보 (미결정, 브레인스토밍용):
  1. **풀블리드 배경** — 상황 이미지를 `object-cover`로 화면 전체에 깔고, 캡션·이전/다음·닫기는 하단 오버레이 (진입 화면과 동일 문법)
  2. **세로 우선 확대** — 9:16 원본 비율을 유지한 채 높이 `dvh`에 맞춤. 좌우 레터박스는 void로 (원본 왜곡 최소)
  3. **탭 시 전체화면** — 목록/작은 썸네일은 유지하고, 선택·「원본 보기」에서만 배경급으로 확대 (기존 갤러리 흐름과 충돌 적음)
  4. **낮 장면과 통일** — DayScreen 상단 장면 컷도 같은 스케일 규칙으로 맞춰, 「열람용」과 「플레이 중」 이미지가 다른 크기로 느껴지지 않게 함
- 주의: 텍스트·버튼 가독성(배경 위 반투명 패널), F7(다음 그림 강조)과 함께 설계하면 확대 화면에서도 페이지 넘김을 놓치지 않기 쉬움
- 조치: 부분 반영 (2026-09-18, 커밋 `b05ffc6`) — 낮 화면 재구성에서 `frontend/components/day/SceneIntro.tsx`가 도입되어 장면 그림이 진입 화면에서 크게 나온다. 적용안 4개 중 최종 결정은 여전히 협의 대기.

기술 점검·빈 세션 판은 이 참가자의 표본으로 사용하지 않는다.
