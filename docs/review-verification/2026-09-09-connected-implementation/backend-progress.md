# Backend progress — 2026-09-09

- Owner: `/root/backend`; scope backend/**, API contract, backend plan/ledger.
- Read root/backend AGENTS and approved connected investigation design.
- Skills applied: writing-plans, systematic-debugging, test-driven-development, verification-before-completion.
- No Git repository. Snapshot `/tmp/connected-backend-before` includes complete backend and API contract.
- Root causes verified: fragments blindly accrued by ordinal; advisor uses lossy model chain and promotes irrelevant detail; custom mapping uses two-character stem matching; rule registration/plan corrections are represented as executed facts; only failed model calls logged; night lacks previous answer endpoint.
- Storage plan: additive event payloads and existing JSON columns; no migrations or production writes.
- API contract published below in docs/spec/api_contract.md; implementation in progress.
- Test ownership: backend agent is sole backend runner; conftest forces pigfarm_test (admin connection only checks/creates that test database).
- Milestone 1: observation DTO/event, disclose/read helpers, scene-timed fragment publication, notes with original sources, previous submitted answer GET, inherited evidence composition implemented. Four new tests confirmed red, then `4 passed in 0.31s`. Local DB requires sandbox escalation; approved test command uses pigfarm_test.
- Contract acknowledgement from frontend: tapped_note_ids FULL current selection; inherited_note_ids ORIGINAL restored IDs unchanged on toggle; attachment removal does not silently rewrite claim prose.
- Next: actual scene action opportunities and rule execution, grounded advisor/custom preview, complete call logs and retrospective.

## Review handoff checkpoint

- Implementation complete for independent review. Full suite: **198 passed, 3 warnings in 5.70s** (`.venv/bin/python -m pytest -q --tb=short`; raw output `/tmp/connected-backend-suite.log`). Warnings are existing FastAPI/Starlette/Google SDK deprecations.
- Architecture: **4 kept, 0 broken**; forbidden-word check: **0 findings / 81 core files**.
- Snapshot diff: `/tmp/connected-backend-review.diff`; exact source file list: `/tmp/connected-backend-changed-files.txt`.
- DB test runner released to root/integration after this checkpoint. Backend owner will not run more DB tests without coordination.
- Milestone 2: finite scenario action opportunities drive actual narration, conditional illustrations, known observation/source disclosure and rule outcome events; no matching opportunity produces no success. Opposing rule loser is conflict; evaluable denominator excludes conflict/unavailable. Paw explanation benefit and one remaining utterance cost originate in the linked execution event, with no cost when remaining budget is zero.
- Milestone 3: advisor reads original public observations, cites exact source text, distinguishes recorded fact/explicit contradiction/unknown and never confirms absent records. Definition/recipient/reason questions require matching source scope. Custom interpretation is a finite grammar with explicit rejection/alternatives; preview token+unchanged original required on apply. No nearest-action rewrite.
- Milestone 4: successful and failed model calls include input/output/attempt/check trace/model/version; retrospective separates story score from measured note linkage/rule opportunities and unreviewed semantic metrics. Historical call counts are null with coverage metadata. Common scoring propositions are explicit (no orphaned “그 두려움”); branch-restricted DTO support is additive. Final truck/quiet text explicitly says closure was not observed; actual closure text only accompanies closure outcome.
- Milestone 5: first-loop-only clue07, actor-guarded clue11, non-actor participant guard for clue08, actual-only 09/10/12 mapping. Final edited answer and current selected IDs survive repeated restoration; detachment changes evidence attachment only, never silently deletes claim prose.

### Existing-test change classification

Initial full-suite checkpoint: 171 passed / 32 failures. These were reviewed rather than bulk changed:

| Superseded expectation | New acceptance protection |
|---|---|
| 21 intervention tests requiring lossy chain confirmation, three forced options, nearest-action translation, or model prompt internals | 10 intervention tests cover original source citations, fabricated-ID rejection, missing-chain recovery, unknown budget accounting, empty viable candidates, contextual reason/evidence, exact action/polarity/recipient rejection, tester1 explicit alternatives, full question log, observed contradiction. New connected tests additionally cover preview token and exact-original apply. |
| One fragment per beat regardless of source | Every note links to a disclosed source; source beat is never future; truck fragment absent until truck outcome. |
| Old static image IDs | Current actual-action image mapping plus suppression and first-loop/co-participant erasure guards. |
| Appended contradictory “today different” narrative and prompt asserting rules already obeyed | Actual changed narration/image and execution events; registration without public observation produces empty cause chain. |
| 18 fixed event samples | 21 typed event round trips, adding observation/execution/preview. |
| Five-loop fixture shape/old generated-side-effect queue | Fixtures add DTO defaults and remove now-unused generated claim call; all scoring/five-loop assertions remain. |

Real regression found while updating fixture: outcome-fragment null matching could disclose fragments when no world outcome existed. Added explicit non-null condition and verified no-record cause chain remains empty.

### Known limits / independent follow-up

- Tests use deterministic fake LLMs; live tester1 question/rule probes and full frontend integration remain root/integration work. Semantic relevance, interpretation quality and checker false-positive/negative rates remain explicitly unreviewed (null metrics, reviewed=0), not model self-certified.
- Custom grammar intentionally supports exact finite actor/action/scene/polarity forms. Generic “answer everything”/ambiguous recipient/unsupported condition requests are rejected with explicit editable alternatives.
- No historical source reconstruction, migrations or regrading. Old observations/call coverage cannot be recovered retroactively.
- Existing repository operations commit independently. Disclosure IDs prevent duplicate original records when re-entered, but there is no new transaction/unit-of-work framework for crash recovery across the whole advance-beat request; independent review should assess this boundary against failure-handling scope.

## Independent review fixes and server handoff

- Applied receiving-code-review skill and reproduced all four reviewer findings before fixes.
- BR1: removed the redundant hardcoded second-loop food/report/rumor fragments. Actual scene records are now their only source. Regression checks all notes and original observation records after suppression.
- BR2: compatible same-polarity rules share one real information action and each records obeyed; duplicate explanation is suppressed. Opposite polarity alone produces conflict. Paw-cost regression verifies one charge for the shared action.
- BR3: new loop-start events carry measurement_version. Note linkage uses only instrumented-loop notes; legacy notes are unmeasured (null, denominator 0, separate unmeasured count). Model-call coverage requires all five loop starts to carry the new version, so mixed historical/new runs cannot claim complete coverage.
- BR4: removed Korean-prefix pre-retrieval filtering. The bounded public five-loop record is available to the advisor. Unknown answers may retain validated original partial evidence, explicitly stating that the requested relationship/reason is unconfirmed. Regressions cover “누가 밥을 남겼어?”, three remaining trays without inferred owners, and broadcast reporting instructions without inventing report completion/recipient.
- Additive next-beat budget_left agreed with frontend and regression-tested after pawn cost (red KeyError → green). Consumer can update its displayed budget immediately.
- **Latest final suite: 204 passed, 3 existing dependency warnings in 6.33s**. Architecture 4 kept / 0 broken; core forbidden words 0 / 81 files. Raw suite log `/tmp/connected-backend-suite.log`. Stable final diff `/tmp/connected-backend-review.diff` refreshed after BR1–BR4.
- Fake integration server restarted only after tests completed: **PID 999291**, exec session 99303, `http://127.0.0.1:8501`. Log `/tmp/connected-backend-8501.log`; launcher `/tmp/connected-backend-8501.py` asserts engine URL database AND SQL current_database() are pigfarm_test and NPC/core providers are fake before starting. Log evidence: `ISOLATION_OK pid=999291 database=pigfarm_test npc=fake core=fake scenario=a port=8501`.
- Operating 8500 server was untouched. Integration now owns pigfarm_test; no pytest reset while this server is in use. Live model tester1 probes still require a distinct recorded verification result and coordination with integration; fake suite is not evidence of real-model answer relevance.

## BR5–BR7 follow-up — stable implementation for independent recheck

Integration explicitly stopped its own 8501 process before handing pigfarm_test back; no participant attempt had been created. Backend ran DB-free regressions first, then owned the database exclusively for the checks below. Production 8500 and production game records remained untouched.

- **BR5 implemented:** the next pawn offer skips an already active overlapping explanation benefit. Existing duplicate pawn rules share one information action and one cost observation; both can be obeyed while only the originating execution carries side_effect and deducts budget. Shared execution cites that cost without charging again.
- **BR6 implemented:** exceptions raised by the LLM provider boundary become an auditable provider-error fallback, rather than escaping and dropping the parallel batch. The error call and successful sibling reports all return and persist. Provider outage does not trigger repeated network retries; malformed structured output retains its existing regeneration policy.
- **BR7 implemented:** SceneTransactionPort is injected only into start_loop/advance_beat. Its SQLAlchemy adapter buffers repository saves as flushes and commits scene state, budget, observations and notes once; any failure rolls back the complete scene. Other request behavior keeps its previous commit policy. Harness audit events inside that scene scope use a separate adapter Session because the model call really happened even if the scene fails; event rows have no loop foreign key.
- Actual PostgreSQL `SELECT 1 / 0` fault injection proves: failed beat2→3 note write leaves beat2 and original budget, no beat3 observations; retry returns beat3 once, one pawn cost, unique original IDs and all matching notes. Provider failure at ambient returns the paid scene with ambient=null and retained failure report. Failed first-scene publication of a new loop leaves no loop2/observations while the completed planner-call audit remains queryable; this also verifies no wait on an uncommitted loop FK and no audit-to-public-evidence promotion.
- New regressions: `backend/tests/pure/test_review_failure_boundaries.py` (4 DB-free tests; red→green), `backend/tests/engine/test_scene_transaction.py` (3 real PostgreSQL tests, including two failure cases reproduced before the boundary was added).
- **Latest complete suite: 211 passed, 3 existing dependency warnings in 5.60s.** Raw output `/tmp/connected-backend-suite.log`. Fresh import-linter: **4 kept / 0 broken, 119 files**. Forbidden words: **0 findings / 83 core files**. Pure-only command: `.venv/bin/python -m pytest tests/pure --confcutdir=tests/pure -q --tb=short` → 4 passed; targeted DB+pure command → 7 passed.
- Final source diff and file list refreshed at `/tmp/connected-backend-review.diff` and `/tmp/connected-backend-changed-files.txt`. BR5–BR7 await independent focused recheck; owner passing tests are not reviewer approval.
- **DB ownership released back to integration after this checkpoint. Backend has no running 8501 process.** Per root instruction integration will launch its own guarded server, so old PID999291/session99303 information above is historical. No subsequent pytest reset is authorized while integration is using pigfarm_test.
- Live Ollama tester1 probes and integrated UI behavior remain distinct pending verification. No live-model semantic correctness claim is made from this fake-provider suite. HTTP response loss after a successful server commit is not covered by the new storage-failure rollback boundary; the requested persistence/provider failure cases are covered.

## Exact custom-target probe follow-up

- Confirmed source from `/root/real_model`: `repr='은상은 소문의 진실을 무조건 나에게 이야기 해준다.'`; old substring matching found 은상 at offset 0 and 준 at offset 25 inside 해준다. Old raw preview was executable=false, limitation `그런 사람은 여기 없다`, alternatives for both 은상 and 준. The earlier claim about the word 질문 was explicitly corrected: `민석이 내 질문에 자세하게 답할 수 있도록 한다.` contains no 준 and already followed the intended unsupported-action path.
- Minimal fix in intervention_interactor.py: shared whole-name matching with optional Korean particles for both target interpretation and alternative generation. Word-internal 해준다/알려준다/보여준다 no longer introduce a person; actual 준은/준이/plain 준 remain valid. Unconditional truth requests now receive the knowledge limitation even when the word order is `진실을 무조건`, rather than `무조건 진실`. Unsupported intentions remain unexecuted and the original text remains unchanged.
- New `backend/tests/pure/test_custom_rule_target_names.py`: 12 cases; original run 7 failed / 5 passed, then all 12 pass. Covers exact raw text+offset, six actor/verb-ending combinations, three real target forms, 질문 control, and two actual named people (still rejected rather than silently reducing the target).
- Post-fix validation: **16 pure tests passed in 0.07s**, fresh architecture **4 kept / 0 broken**, forbidden words **0 / 83 files**. Command `.venv/bin/python -m pytest tests/pure --confcutdir=tests/pure -q --tb=short` intentionally skips DB fixtures. The last full DB suite remains the prior 211-pass checkpoint; it was not rerun while integration owns pigfarm_test. No DB/server writes or resets occurred in this follow-up.
- Exact-preview recheck requested from real_model and independent review requested through root. Final combined diff/file list refreshed at the same `/tmp/connected-backend-review.diff` and `/tmp/connected-backend-changed-files.txt` paths. This change adds no API fields or migration.

## Real-model grounding repair — final backend checkpoint

Confirmed raw source: `/tmp/pigfarm-connected-integration/ollama-results/probes-20260909T035804Z.json`. All 12 Gemma questions exhausted three attempts because optional detail was nevertheless required to repeat exact original text. Exaone's accepted free-text dialogues invented a food preference, ration numbers/record completion, a smile and previous absence. These were genuine defects despite earlier fake-provider checks.

- Advisor validates public IDs, resolves exact source text itself and ignores model detail. Unknown keeps relevant original records via supplemental finite topic recall (the model still receives all public records), preserving loop and observed/reported labels. No model text is promoted to a confirmed note. Source membership is not relationship proof: unresolved role/definition/recipient/return/outcome questions stay unknown while retaining partial evidence. Capability questions explain actual notebook/question/rule-preview operations directly.
- Ambient output is now internal candidate_index selection, never generated dialogue. Server candidates quote only current loop/beat observations with an explicit observation/statement label; old ambient is excluded. Present speakers alone are used and candidates violating current ban/erasure rules are omitted. No candidate or explicit null means no dialogue; provider/shape failure uses the first safe candidate. Memory stores the actual scoped utterance, not an invented experience. The same original statement and note are committed atomically with the scene.
- New `tests/pure/test_real_model_grounding.py` has 21 regressions: missing detail, malicious detail, bad IDs with partial fallback, three trays/unknown owners, instruction/approach versus report completion/recipient, capability scope, term/relationship/role/outcome unknowns, three positive controls, three invented dialogue replays, reported-scope candidate and old-scene exclusion. First 12 all failed before repair. Added positive-control statement occurrence initially failed and was fixed without claiming the reported content itself is true.
- Full-suite checkpoint: **232 passed / 12 failed** after initial repair. Eleven failures were superseded free-text/null-fallback/prompt expectations; these now test actual candidate selection, source/speaker membership, shared memory, gauge and event invariants, actual suppression and no plan-as-fact promotion. One actual regression (ambient observations lacked note attachment) was fixed inside the scene transaction; the original rollback/retry/all-source-notes regression remains unchanged. Next checkpoint 243 passed / 1 failed exposed an incorrectly expected suppressed-caption string in the new assertion; it now matches the actual scenario statement.
- **Final complete suite: 244 passed, 3 existing dependency warnings in 6.33s; exit 0.** `/tmp/connected-backend-suite.log` holds the final result. Pure-only suite **37 passed**. Fresh architecture **4 kept / 0 broken, 119 files / 230 dependencies**; forbidden-word scan **0 / 83 core files**. One scenario-specific recall keyword discovered by that scan was removed before final suite. Initial sandbox run could not connect to PostgreSQL and produced setup errors; authorized escalated runs above completed normally against pigfarm_test.
- Integration explicitly stopped its own 8501 process and handed back pigfarm_test before full DB checks. Backend now releases that DB ownership, with no backend server running. Production DB/8500 were untouched. Final diff and exact file list refreshed. The earlier ambient=null outage statement above describes the previous checkpoint, superseded by the safe-candidate behavior here.
- Independent semantic/code recheck and fresh real-model Q12 + three positive controls + three actual `_make_ambient` probes remain separate work. Finite recall/relationship guards are conservative and are not a general semantic proof system; those live results must assess usefulness. HTTP response loss after a successful commit remains outside the database failure rollback guarantee. No migration or historical regrade.

## RM1–RM3 independent real-model review repair (verification in progress)

Evidence: durable real-model-artifacts/probes-20260909T041723Z.json and latest real-model-review.md. Valid citations alone did not establish question support: live PC1/2 were wrongly unknown and PC3 positive publication question was wrongly supported by an explicitly negative source. Q1/truck topic expansion added irrelevant records, Q11 retained an unrelated valid truck ID, and next paths ignored excluded actors/source timing. Actual A3 was incorrectly filtered as unknown person `말이` before any model call.

- Internal output now requires relation plus evidence_ids; HTTP answer/status derives from the question proposition's relation. Prompt examples distinguish both polarities of a publication question, actual statement occurrence versus execution, and observed action versus hidden reason. Server resolves exact source text; private/future IDs are rejected and reported content cannot prove execution. Source negation is no longer required for contradiction: a positive record can contradict a negative question.
- Related selected IDs are retained without blanket expansion. Finite correction separates truck from transfer, narrows food evidence and respects excluded people. Only an empty relevant selection in unknown triggers narrow recall; no-topic/no-actor fallback does not dump all records. Next path uses actual evidence loop/title/actor and never describes old records as just witnessed.
- Ambient candidates are server quotations of current disclosed sources, so speculative free-generation unknown-person checking was removed from this path only. Erased-character/current-scene/forbidden-word/language checks remain. Exact `들은 말이야` now reaches selection and scoped storage.
- New tests/pure/test_relation_review.py: initial 8 failed / 1 passed; repaired and added reverse-polarity positive-source case. **47 pure tests pass**. Existing question fake fixtures now explicitly use the relation schema; narrow evidence expectations were changed where the independent review required removal of excluded/irrelevant material. No HTTP fields or migrations changed.
- Full DB suite pending explicit integration handoff; no DB or server operation has occurred in this resumed repair yet. Fresh live-model 15 questions + 3 custom + 3 ambient + bidirectional negative controls requested separately. Passing fake tests do not establish the previously failing live positive controls.

## Same-event scope follow-up after authorized 05:41:43 live probe

Root's authorized run (probes-20260909T054143Z.json) produced 18 real outputs with no provider errors/fallback. RM2 evidence/paths and RM3 actual ambient passed; only PC3 remained: positive publication question was unknown despite the selected record explicitly saying the result was not public. Its negative counterpart PC4 was supported at the same source/time. Earlier provider-blocked 04:40 raw is not a live semantic result.

Minimal hypothesis: relation-first structured generation committed before selecting the source, while original prompt context omitted actor/loop/scene metadata and did not explain multi-sentence omitted-subject resolution. Implemented evidence_ids-before-relation field order; included exact source actor, loop, beat and scene title; added a generic multi-sentence inspection example contrasting disclosure occurrence, undisclosed contents, reason and future. A continuous scene may supply the omitted subject; ambiguous or changed event/time remains unknown. There is no PC-specific answer override, generic negative-character inversion, new model call, or HTTP/loop/score change.

- New tests/pure/test_relation_event_scope.py: 2 failures reproduced (generation order and source metadata), plus 5 retained relation/scope controls covering positive/negative publication, content, reason and future. Tests do not assert copied prompt-example wording. They prove the transport/schema/scope contract; they do not certify actual model reasoning.
- **54 pure tests passed in 0.09s**, fresh architecture **4 kept / 0 broken, 119 files / 230 dependencies**, forbidden words **0 / 83**. No DB/model/API/server calls or escalated tool requests were made in this follow-up. Root's latest full DB result before these changes is 254 passed (separate root-run evidence); current full DB and unchanged 16-question live harness reruns belong to root's authorization relay.
- Focused before/after copies: `/tmp/connected-rm1-scope-before` and `/tmp/connected-rm1-scope-after`. Focused diff `/tmp/connected-rm1-scope.diff`; current source hashes `/tmp/connected-rm1-scope-sha256.json`. Full original-backup diff and changed-file list refreshed separately. Root received production file hashes before rerunning anything.
- This hypothesis remains subject to the identical live positive/negative controls and independent review. The remaining PC3 is not declared fixed on fake tests alone.

## Polarity decomposition after 05:54:16 PC4 regression

Independent real-model review found PC3 corrected but the negative PC4 incorrectly contradicted against the same explicitly unpublished result. Prompt wording/schema order alone therefore did not close RM1. Root and reviewer agreed to separate semantic tasks while preserving directly answerable open questions.

- Model output now requires evidence_ids, question_kind (proposition/lookup), positive_claim (audit-only canonical positive proposition or concrete lookup answer), evidence_polarity (affirmed/denied/unknown relative only to that proposition), and question_polarity (positive/negative/open). Subject, event, time and quantifiers are preserved; only the main predicate polarity is normalized, not negation inside quotations/subordinate clauses. There is no direct model relation field.
- `question_relation()` composes the four cases. Positive/affirmed and negative/denied are supported; positive/denied and negative/affirmed contradicted. Unknown evidence or null claim never becomes known. Lookup is not inverted: an affirmed concrete requested value/action is supported; absent or denied candidates are unknown. An observed who-did-it lookup and an explicitly recorded reason remain answerable. Missing reason/content/future/condition information remains unknown; the original public-ID, relevance, event/scope and reported-truth guards remain after composition.
- Only original source text is rendered. Model positive_claim remains in complete call audit. No Korean negative-word scanning, PC string special case, extra model invocation, HTTP status redefinition, scoring/loop/server/DB change was introduced.
- New `tests/pure/test_question_polarity.py`: initial 7 failed/8 passed; now 17 pass including four matrix cases, unknown invariance, direct who lookup versus missing why/content/future/condition, explicit reason lookup, denied lookup, null claim, subordinate negation and speech occurrence versus execution. Existing fake-provider fixtures were migrated to explicit new semantic axes; dedicated negative-pair fixtures use the same positive claim and source polarity for both questions. The old relationship schema assertion was replaced by required axis assertions. These fixtures verify composition/boundaries, not real model interpretation.
- **71 pure tests passed in 0.10s**. Fresh architecture **4 kept / 0 broken (119 files, 230 dependencies)**; forbidden words **0 / 83**. Root's previous full 261 suite remains a prior-code checkpoint. No new DB/model/provider call or escalated permission request was made by this backend agent; root relays those executions.
- Focused artifacts: `/tmp/connected-rm1-polarity-before`, `/tmp/connected-rm1-polarity-after`, `/tmp/connected-rm1-polarity.diff`, `/tmp/connected-rm1-polarity-sha256.json`. Current source hashes were sent to root before rerun. Combined original-backup diff/file list refreshed. Corpus of 12 questions, four original positive controls, three custom previews and three production ambient paths remains intact; live direct-who/reason/future/conditional/negation-scope controls were requested additionally.
- **RM1 remains pending live/independent approval.** A correct code truth table does not prove the model extracted the correct claim, evidence polarity, question kind or negation scope. Review must inspect those raw fields alongside each final HTTP relation.

## Two-stage question/evidence separation after 06:10:38 review

The previous single-call decomposition still misread synthetic published evidence as denied under a negative question (SPC2); lookup reasons became known actions and future/actual-execution requests lost their scope. Root requested isolation of question interpretation from public records, rather than another prompt-wording adjustment. Reviewer retains RM1 P1 until the unchanged expanded live corpus passes semantic review.

- `AdvisorInterpretationOutput` receives only the original question: kind, proposition-only positive_claim, polarity. Lookup must keep a null claim/open polarity, so it cannot be rewritten into a known action. Proposition normalization preserves explicit time/condition/quantifier scope; missing explicit markers such as tomorrow/next trigger interpretation retry. This is scope validation, not negative-word scanning or answer hardcoding.
- `AdvisorEvidenceOutput` receives binary normalized positive_claim plus public records only, with no original negative question or question polarity. Lookup receives the original question unchanged plus public records and never asks the model for a replacement claim. It returns original evidence IDs and affirmed/denied/unknown only. Existing four-case composition, lookup no-inversion, exact source reconstruction, relevance/unknown/statement-truth guards remain.
- Each actually invoked phase is logged with its own harness role: advisor_interpretation and advisor_answer. Interpretation failure prevents the dependent evidence call; second-phase failure retains both audit reports. Partial original records remain available under unknown. Original question/event/remaining budget are updated once after the whole request; meta and no-public-record shortcuts still make no model calls.
- New tests/pure/test_question_stage_isolation.py: initial **9 failures**, then 11 pass including input separation, identical normalized claim for four opposite-source/polarity cases, unchanged who/why lookup, each provider failure stage, time-scope retry before consulting evidence, and rejecting lookup-to-action rewriting. Checks explicitly assert one question cost, original query log, two success audit records, or the exact failure call count. Existing fake fixtures now emit separate phase responses; original source checks move to evidence-stage input, not interpretation input.
- **82 pure tests passed in 0.13s**. Fresh architecture **4 kept / 0 broken (119 files, 230 dependencies)**; forbidden words **0 / 83**. Updated DB fixture modules compile. No DB, actual-model/provider, API/server or escalation request was made by this agent. Root relays full suite and unchanged actual expanded 12+10+3+3 corpus. Prior full suite counts remain previous-code evidence until that relay completes.
- Focused artifacts: `/tmp/connected-rm1-two-stage-before`, `/tmp/connected-rm1-two-stage-after`, `/tmp/connected-rm1-two-stage.diff`, `/tmp/connected-rm1-two-stage-sha256.json`. Root received production hashes before executing. Full original-backup diff/file list regenerated; this scope adds no HTTP field, migration, loop/score/ambient/custom change.
- Passing isolation tests prove which inputs reached which role and how supplied evidence is composed; they do not prove the model normalized the real question or judged the real source correctly. **RM1 remains pending actual corpus and independent semantic review.**

## Coherent interpretation enum after 06:28:38 real-model failure

Read the exact 285,026-byte 062838 raw and independent review. Interpretation repeatedly produced the allowed but incoherent proposition/open combination; PC7 dropped the explicit tomorrow scope. PC4 received the correct multi-sentence unpublished source but returned empty IDs/unknown. PC6 correctly remained a why lookup yet classified the action-only record affirmed. PC8 misclassified a positive execution question as negative. Input isolation was intact. These are model semantic/schema failures, not transport or production-data failures; RM1 remains P1 pending the same expanded corpus.

- Replaced independent question_kind/question_polarity with required question_type enum (positive_proposition, negative_proposition, lookup), retaining proposition-only positive_claim. The old proposition/open combination is now schema-invalid. The four-case composition, unchanged lookup original, scope checks, two separate audit roles, one question cost and all original source/truth guards remain.
- Both advisor classification harness calls explicitly use temperature=0 (previous raw recorded None, adapter default 0.7). Global NPC/ambient settings are unchanged. Classification prompt distinguishes main predicate polarity from interrogative form/unknown answer and preserves original temporal/conditional text. Evidence schema descriptions and lookup examples distinguish the requested answer from a merely related observation. This is no guarantee of correct real-model reasoning.
- PC8's empty-unknown response also exposed a separate narrow recall gap: '방송 지시' did not match the '방송실' cue. A regression failed with an empty evidence list, then passed after using 방송 as the existing broadcast-topic cue. This preserves the original instruction as partial evidence while keeping actual execution unknown.
- Test execution checkpoints this follow-up: stage tests **12 failed / 2 passed** (new schema/temperature red); whole pure **2 failed / 83 passed** (two stale required-schema assertions); stage **1 failed / 14 passed** (new broadcast partial-evidence red); final whole pure **86 passed in 0.12s**. These are four distinct pytest invocations, not sums of cases. Fresh architecture **4 kept / 0 broken**, forbidden words **0 / 83**. No DB/model/API/server or escalation calls by this agent. Root's prior full **289 passed in 6.61s** belongs to the previous two-stage code.
- Changed production: llm_output_dto.py and intervention_interactor.py; fixture/schema/isolation tests: test_real_model_grounding.py, test_relation_review.py, test_question_stage_isolation.py. Focused before/after snapshots `/tmp/connected-rm1-enum-before` and `/tmp/connected-rm1-enum-after`, diff `/tmp/connected-rm1-enum.diff`, hashes `/tmp/connected-rm1-enum-sha256.json`. Full original-backup diff refreshed. HTTP/loop/score/custom/ambient schemas and behavior were not changed.
- Root received stable production hashes before authorized full suite and unchanged 12+10+3+3 real-model rerun. Pure tests verify matrix/schema/input/cost/log/source boundaries only. Semantic acceptance remains explicitly open for PC4 source inference, PC6 requested reason, PC7 scope and PC8 polarity as well as the entire retained corpus.

## Whole-claim entailment after 06:38:51 live failure

Read exact `probes-20260909T063851Z.json`. PC3 returned denied with empty evidence IDs three identical times at temperature0, triggering fallback. PC4/SPC2 interpretation retained the negative phrase inside positive_claim; the second phase classified denied and the polarity matrix made SPC2 incorrectly supported. PC4's apparently correct final status depended on two errors. PC7/8 interpretation improved; PC6 still called an action-only source a reason answer internally, with unknown guarded externally. Root's full **293 passed in 6.84s** is this preceding enum-code checkpoint, not semantic approval.

Root explicitly selected the simpler alternative: preserve the complete proposition instead of converting its polarity twice. `AdvisorInterpretationOutput` now returns claim plus proposition/lookup; a proposition keeps its original negation, time, condition, quantifiers and quoted clauses. Lookup retains null claim and passes the original question unchanged. `AdvisorEvidenceOutput` returns evidence_ids and supported/contradicted/unknown relative to the whole claim (or actual answerability for lookup). Removed question_type, evidence_polarity and the four-case composition function. Existing public-source reconstruction, time/lookup scope validation, relevance, unknown and reported-truth guards still apply. A contradicted lookup is not treated as a direct answer. No regex rewrites or answer-specific hardcoding were added.

The harness now supports opt-in validation-error feedback; only the two advisor calls enable it. Parse/schema/fact failures append the actual error for the next attempt, and each call audit captures the changed input. The interpretation scope message identifies the original missing time/quantifier marker without public records. Evidence missing-ID feedback lists only IDs already in its permitted public records. Provider failure behavior and retry limit are unchanged. Other roles retain identical retry inputs. Calls remain temperature0, and the full question request costs one budget unit even with two phases/retries.

Verification and coverage migration:

- Stage regressions first failed **11 / passed4** (new direct DTO and feedback absent). After implementation **4 failed /11 passed** because four new generic claim tests accidentally used a 검진 query with source text lacking that topic; corrected their query to the actual tested claim, without changing product relevance guards. Whole pure then **86 passed in 0.16s**; after explicit non-advisor feedback isolation coverage, final **87 passed in 0.14s**. Four distinct pytest invocations in this follow-up.
- Previous matrix fixtures were replaced with explicit positive and negative whole claims and their directly supplied relation. Opposite-source expectations are unchanged (unpublished/public vs positive/negative questions). Retained unknown, direct-who vs absent-why, explicit-reason lookup, future/conditional/content, subordinate negation, null claim and reported occurrence/execution controls. These tests exercise source transport and direct status handling, not actual model reasoning. Stage checks retain one budget cost, two phase audits, first/second provider failure, input isolation, scope retry, source-ID retry, lookup preservation and partial broadcast sources.
- Fresh architecture **4 kept /0 broken (119 files,230 dependencies)**; forbidden words **0 /83**. No DB/model/provider/API/server/escalation execution by this agent. Root received three production hashes and code/test freeze before relaying full suite and unchanged actual corpus; the helper/corpus was never edited by backend.
- Focused before/after snapshots `/tmp/connected-rm1-entailment-before` and `/tmp/connected-rm1-entailment-after`; diff `/tmp/connected-rm1-entailment.diff`; exact hashes `/tmp/connected-rm1-entailment-sha256.json`. Production changes: DTO, intervention interactor, harness. Tests: five pure files and two engine fixture files. Full original-backup review diff refreshed. HTTP/loop/scoring/ambient/custom behavior is unchanged.

**RM1 remains open until independent actual semantic review.** Removing redundant polarity transformation eliminates that code path; it does not prove the model preserves each original claim or evaluates every negative/source pair correctly. Same original12 + expanded10 controls + custom3 + actual ambient3 remain the acceptance corpus.

## Source-local extract-first assessment after bounded 07:07:29 diagnostic

The immutable twelve-call diagnostic reproduced PC3 unknown under original and concise global-relation prompts. Extract-first alone matched all four expected relations with verbatim quotations, but PC3/SPC1 included both a reported instruction and a direct observed result. The existing all-selected-records reported veto would still replace those correct relations with unknown. This diagnostic did not test source-local relations or the full corpus and is not final product acceptance.

Root approved three simplifications: first-stage question_kind only (no claim rewriting); original question delivered unchanged to the second stage; per-source `{id, quote, relation}` extraction followed by eligible-source aggregation. This removes the path that changed 열이나면 into 열리면, dropped why, or normalized negation. The first classification can still be wrong, but it cannot replace the user's text. Two actual harness roles, one question cost, temperature0, retry feedback and original question audit remain.

- Schema extracts each source's ID and nonempty quote before its relation. The checker rejects invalid IDs, whitespace-only or fabricated quotations and conflicting relations for a duplicate ID. Identical duplicates are deduplicated. Source text is reconstructed from the original record, never from the model's quote or paraphrase.
- Related selected records remain context. Only observed sources, or reported sources for the existing speech-occurrence scope, are eligible to determine the relation. `unresolved_question` is applied independently to each source's complete original text. Thus a related action-only row cannot invalidate a separate explicit-reason source, and a reported instruction cannot veto a direct observation. No new topic/negation keywords were added. Ineligible/unknown rows contribute no relation; one remaining direction decides; opposite eligible directions or no eligible relation remain unknown. A contradicted lookup remains unknown.
- New `tests/pure/test_source_assessments.py` has **14 passed** cases covering reported context with all three supplied relation values, reported-only truth refusal, reported speech occurrence, bad ID, empty/fabricated quote, conflicting duplicate, identical duplicate, opposite observed evidence, per-source why scope, lookup contradiction and original question preservation. Existing stage/polarity fixtures now validate original text rather than a rewritten claim; retired claim-normalization and marker-loss tests are replaced by spelling/why/future/execution original-input checks. Core helpers provide explicit original-source quotes to fake models; no real corpus/helper file was edited.
- Exact pytest invocations in this follow-up: one command failed with missing test path and **no tests ran** (0cb146); initial **11 failed/3 passed,0.11s** (3bb058); accidental unchanged retry after an edit path error **11 failed/3 passed,0.10s** (6531b8); focused **14 passed,0.08s** (9b2637); final whole pure **100 passed,0.16s** (00c74d). These are five invocations including the zero-collection command, not five passing runs. Fresh architecture **4 kept/0 broken** (c01e96,119 files/230 dependencies), forbidden words **0/83** (ec12d9).
- No DB/model/provider/server/escalation execution by backend. Root received stable DTO/interactor hashes and code/test freeze before full-suite and unchanged expanded-model relay. Focused before/after snapshots: `/tmp/connected-rm1-source-before`, `/tmp/connected-rm1-source-after`; diff `/tmp/connected-rm1-source.diff`; exact hashes `/tmp/connected-rm1-source-sha256.json`. Full original-backup diff/file list refreshed (39 files). Product HTTP schema, loop/scoring, custom and ambient behavior remain unchanged.

**RM1 semantic approval remains pending.** Verbatim quotes do not prove each source's model-assigned relation is correct. This new aggregation also deliberately requires individual sources to establish the requested relationship; it does not infer an unstated relationship by combining partial facts across records. The identical original12 + controls10 + custom3 + production ambient3 corpus remains the next evidence.

## Final bounded prompt update and code freeze

Root set a final bounded stop: apply only the two evidence-backed diagnostic prompts, run the existing pure suite and one final full-suite/model relay, then report remaining errors without another development/diagnostic cycle. The kind-only diagnostic candidate classified all21 actual questions correctly. Explicit yes/no evidence semantics corrected PC3 and future PC7 in the seven-call diagnostic, but **SPC2 still incorrectly supported the negative question against the published source, and PC6 still assigned supported to action-only why evidence**. This is a known limitation, not a solved semantic gate.

Copied the exact diagnostic CLASSIFY text and COMMON/LOOKUP/PROPOSITION texts into the existing interactor. No schema, source validation, source aggregation, original-question handling, loop/scoring, HTTP, custom, ambient or test changes. The first prompt distinguishes yes/no completion from a requested person/reason/value with six generic examples. The second distinguishes finding an answer from the truth of the question's content using four generic positive/negative source/question pairs, current/future scope and action/reason lookup examples. No corpus question appears as a hardcoded answer.

- An initial edit construction failed syntax compilation before writing the file; an already-batched pure command therefore ran against the unchanged prior source (**100 passed,0.12s**,355a6c). After correcting only the patch construction and saving the exact strings, the **single current-code pure run passed100 in0.13s** (e87d4e). No tests were added. These are two invocations, with their different source checkpoints explicitly distinguished.
- Current interactor SHA-256: `6fb42a732b4def2fb24892a7e741cb4593271de4004c976ade78a51d03a2d8d9`. Root received this hash and a code/test freeze before its final full/model relay. Latest prior architecture4/4 and forbidden0/83 remain applicable dependency checks; this one-file text change adds no imports. Backend did not call DB, models, API/server or escalation tools.
- Focused final artifacts: `/tmp/connected-final-prompts-before`, `/tmp/connected-final-prompts-after`, `/tmp/connected-final-prompts.diff`, `/tmp/connected-final-prompts-sha256.json`. Combined original-backup diff refreshed; changed-file list remains39. The previous source-local manifest is an earlier checkpoint and must not be mistaken for this final interactor hash.

**Code frozen; final live semantic outcome is pending root's single relay and independent review.** If SPC2 or other semantic errors remain, report them accurately, retain the existing production server version, and identify the unresolved product/model capability decision. Do not describe passing structural tests as full functional or semantic completion.
