# 휴먼테스트 기록 — 테스터9

- 참가자 ID: 테스터9 (사용자 지정)
- 상태: 5회차 완주 (clear) — **도움받은 플레이**(낮 질문·추리문·신의 질문·규칙 선택을 Claude가 추천, 사람이 입력). 사람이 혼자 추리한 표본과 섞어 비교하지 않는다.
- 일시: 2026-09-17 11:43 ~ 15:00 KST
- 접속: http://localhost:3500/play (개발 계정)
- attempt_id: `e6290e17-8f36-4d3c-b134-14fb892d5016`
- 인스펙터: http://localhost:3500/inspector/e6290e17-8f36-4d3c-b134-14fb892d5016
- 기기·화면 크기: 미확인
- 오염 요인: Claude는 F11 조사 중 채점 기준 문장을 열람했고, 이후 추천(특히 3일째 "이송될까 봐 숨긴다"·5일째 정체 명시 문장)에 영향이 있었을 수 있다. 유도 질문 입력 사례(F11)·DB 조각을 화면 정보로 오인한 추천(F23)도 포함.

## 회차별 결과 (DB `nights` 읽기 전용 조회)

| 회차 | 총점 | 통과 | cause | motive | identity | side_effect | 비고 |
|---|---:|---|---:|---:|---:|---:|---|
| 1 | 55.4 | O | 75.0 | 83.3 | 0 | 0 | 근거 4개 선택 |
| 2 | 64.2 | O | 100 | 83.3 | 0 | 0 | cause-5가 방송 조각으로 오인정(F11 재정정) |
| 3 | 55.4 | O | 75.0 | 83.3 | 0 | 0 | 추리문 새로 씀, 근거 조각 후보 제외 |
| 4 | 55.4 | O | 75.0 | 83.3 | 0 | 0 | 사실 나열만으로는 identity 0 |
| 5 | 85.4 | O | 75.0 | 83.3 | 100 | 0 | 정체를 명시한 마지막 문장 1개가 identity-1·2 모두 인정 |

- 규칙: R1 채연 설명(원숭이손)·R2 민석 수첩 보여주기·R3 민석 설명(원숭이손)·R4 준 밤에 깨어 있음·R5 준 설명·R6 준 포대 들여다보기.
- 신의 질문 판정 오류(F10) 이 판에서 4건. side_effect는 끝까지 0(테스터6·7과 동일).

## 사용자 피드백 원문

분류: 프론트엔드/UI(화면·문구·사운드 표시) · 백엔드/AI(서버 설정·모델 판정·채점). F 번호는 접수 순서를 유지한다.

## 프론트엔드/UI

### F1 — 로그인 안내 문구가 무례하고 "랜딩"이라는 용어가 낯섦 (2026-09-17)

- 장면: `/play` 첫 진입 — 로그인하지 않은 상태의 안내 화면
- 요청:
  > 일단 들어가자마자 "로그인이 필요하다. 랜딩에서 구글로 시작해라."라고 뜨는데 너무 무례하고, 사용자 친화성을 고려하면 '랜딩'이라는 단어도 안 쓰는 게 좋을 것 같아
- 해석: 로그인 안내가 명령조 반말이라 첫인상이 불친절하다. "랜딩"은 개발 용어라 플레이어에게 쓰지 않는다. 안내 문구를 정중하게 바꾸고 "랜딩" 표현을 뺀다.
- 코드 위치: `frontend/components/screens/GuardScreen.tsx` — `MESSAGE.login` 문구와 버튼 라벨 "랜딩으로". 같은 파일의 하루 한도·정원 마감·속도 제한 문구도 같은 반말 어조다(요청 범위 밖, 참고).
- 조치: 반영 (2026-09-17 밤, 프론트 커밋 `b05ffc6`) — 가드 화면 문구를 존댓말로, "랜딩" → "처음 화면"(`GuardScreen.tsx`, `guard.cjs` 기대값). 하루 한도·정원 마감·속도 제한 문구도 같은 어조로 맞춤.

### F3 — 랜딩 버튼 등장 시점에 BGM 시작·우상단 BGM 토글 표시 (2026-09-17)

- 장면: 랜딩 — 소개 문구 2줄 뒤 버튼(로그인·시작)이 나타나는 시점
- 요청:
  > 랜딩 화면에서 소개 문구 2줄 뜬 다음 버튼들이 뜨잖아. 지금은 랜딩 화면에 BGM이 없는데 버튼이 뜨기 시작하는 시점에 BGM이 시작됐으면 좋겠어. 그리고 그때 화면 우상단에 토글 스위치도 나타나도록 하면 좋겠어
- 해석: 랜딩에 BGM을 추가하고, 첫 버튼 등장(`animationDelay` 4.2s)에 맞춰 재생을 시작한다. 같은 시점에 우상단에 BGM 토글 스위치를 페이드인한다.
- 코드 위치: `frontend/app/page.tsx`(버튼 `entry-line` 4.2s·4.6s), `/play`의 기존 BGM 재생·`ToggleSwitch`(`frontend/app/play/page.tsx`), 켜기/끄기 저장 `frontend/lib/audioSettings.ts`(랜딩·게임 공유 값).
- 참고: 브라우저 자동재생 정책상 사용자 조작 전 첫 방문에서는 소리 재생이 막힐 수 있다. `/play`처럼 첫 클릭·키 입력에서 재시도하는 방식이 필요할 수 있다.
- 조치: 반영 (2026-09-18 17:34, Codex astra, 계획 `docs/superpowers/plans/2026-09-18-tester9-remaining.md`) — 랜딩에서 로그인 버튼 등장(4.2s `onAnimationStart`)에 맞춰 기존 곡 `/audio/game-bgm.mp3` 재생·우상단 BGM 토글 페이드인, 저장된 끄기 설정 존중, 자동재생 거부 시 첫 조작에서 재시도. 스크린샷 `15-landing-bgm-toggle.png`.

### F4 — '플레이 안내'와 '단서 기록 열기'를 한 곳에 모으고 더 눈에 띄게 (2026-09-17)

- 장면: 낮(Day) — 화면 상단 "플레이 안내" 접힘 메뉴와 그림 아래 "단서 기록 열기" 버튼
- 요청:
  > 실서비스할 것을 고려하면 지금 UI가 상당히 난해해서 정비할 필요가 있어보여. '플레이 안내'랑 '단서 기록 열기'를 한 곳에 모으고 좀 더 눈에 띄게 만들면 좋을 것 같아.
- 해석: 도움말(플레이 안내)과 기록(단서 기록)이 화면 위·가운데로 흩어져 있어 찾기 어렵다. 두 진입점을 한 위치(예: 고정 메뉴 영역)로 모으고 시각적으로 강조한다.
- 코드 위치: `frontend/components/screens/DayScreen.tsx` — 상단 `<details>` "플레이 안내"(`GameplayGuide`)와 그림 아래 "단서 기록 열기" 버튼.
- 관련: 테스터6 F8(단서 기록 열기의 발견성).
- 조치: 반영 (2026-09-18 오후, 낮 화면 재구성 — 계획 `docs/superpowers/plans/2026-09-18-day-screen-vn.md`, 설계 `specs/2026-09-17-day-screen-vn-design.md`, 프론트는 Codex CLI) — 상단 HUD [기록·안내] 버튼 하나로 단서 기록·플레이 안내(+걸린 규칙) 두 탭. 옛 접힘 안내와 '단서 기록 열기' 버튼 제거. 1일째 진입 화면에는 안내 4줄을 펼쳐서 표시(사용자 요청으로 축약).

### F5 — 인물 대사를 채팅창 대신 비주얼노벨 문법으로 (2026-09-17)

- 장면: 낮(Day) — NPC 대화
- 요청:
  > 지금은 인물들의 대사가 채팅창처럼 보여지는데, 사용자에게 익숙한 기존 비주얼노벨 문법으로 가는 게 어떨까 싶어(풀스크린 일러스트 + 대사창 + 그 밑에 입력창).
- 추가 요청:
  > 채팅창에 한 번에 나와있는 글들을 줄 단위로 쪼개서 비주얼노벨 대사창에서 한 줄씩 넘길 수 있게 하면 좋을 것 같아

  > 다만 기존 비주얼노벨과 다른 점은 대사를 일방향으로 넘기는 것뿐 아니라 이전으로 이동해서 지난 대사도 볼 수 있음
- 해석: 대화 기록이 쌓이는 채팅 로그 대신, 풀스크린 일러스트 위에 현재 대사 한 개를 보여주는 대사창을 두고 그 아래에 입력창을 붙인다. 한 번에 여러 줄로 오는 응답(NPC 대사·관리자 방송·시스템 문구)은 줄 단위로 나눠 대사창에서 클릭·키 입력으로 한 줄씩 넘긴다. 일반 비주얼노벨과 달리 앞으로만 넘기지 않고, 이전 줄로 되돌아가 지난 대사를 다시 볼 수 있어야 한다(대사창 안에서 앞·뒤 이동).
- 코드 위치: `frontend/components/screens/DayScreen.tsx` — 대화 로그·입력 영역(하단 `grid` 입력 줄 포함).
- 설계 시 확인: 줄을 나누는 기준(개행 vs 문장 부호), 마지막 줄까지 넘기기 전 입력창 잠금 여부, 줄별 음성(`voiceForLine`) 재생과의 맞물림, 뒤로 이동할 수 있는 범위(현재 응답 안 vs 같은 인물과의 이전 대화·다른 인물 대화까지), 뒤로 간 상태에서 새 입력 시 최신 줄로 복귀 여부.
- 조치: 반영 (2026-09-18 오후, 낮 화면 재구성 — 계획 `docs/superpowers/plans/2026-09-18-day-screen-vn.md`, 설계 `specs/2026-09-17-day-screen-vn-design.md`, 프론트는 Codex CLI) — 인물 대사를 채팅 로그 대신 대사창(`LineBox`)에 한 줄씩. 인물 카드 4칸(이름만)·선택 인물 큰 초상, ◀로 오늘 하루 전체 줄 거슬러 보기, 마지막 줄까지 봐야 입력 열림. 대화 이력은 대사창 ◀와 기록 패널로.

### F6 — 한 화면 정보량 과다: 장면 그림은 처음에만 보여주고 이후 화면은 인물 상호작용만 (2026-09-17)

- 장면: 낮(Day) — 상단 장면 그림 + 인물 선택 + 옆 채팅창·입력창이 한 화면에 동시에 있음
- 요청:
  > 한 화면에 너무 정보량이 많아(화면 상단에 그림 + 인물 선택 기능 + 그 옆에 채팅창과 입력창). 화면 상단 그림은 처음에 보여준 다음 '단서 기록 열기'에 넣어두고 그다음 화면에는 인물들이랑 상호작용만 할 수 있도록 하면 좋을듯
- 해석: 장면(beat) 진입 시 그림을 먼저 단독으로 보여주고, 넘기면 그림은 단서 기록에서 다시 볼 수 있게 보관한다. 다음 화면은 인물 선택·대화만 남긴다.
- 코드 위치: `frontend/components/screens/DayScreen.tsx` — 상단 장면 그림·일러스트 캡션 넘김 영역과 인물 선택·대화 영역.
- 관련: F5(비주얼노벨 대사창)와 한 화면 구성으로 묶어 설계해야 한다. 테스터7 F2(장면에 없는 NPC 딤)·F3(NPC 선택 2×2 배열)과도 같은 화면이다.
- 조치: 반영 (2026-09-18 오후, 낮 화면 재구성 — 계획 `docs/superpowers/plans/2026-09-18-day-screen-vn.md`, 설계 `specs/2026-09-17-day-screen-vn-design.md`, 프론트는 Codex CLI) — 장면 그림은 도입(`SceneIntro`)에서만 크게, 대화 모드는 인물·대사창만. 기록 패널의 줄에서 그림 다시 보기.

### F7 — 단서 기록을 카드 대신 한 줄씩 로그 형식으로 (2026-09-17)

- 장면: 낮(Day) — "단서 기록 열기" 대화상자
- 요청:
  > 단서 기록이 지금은 카드 형식으로 되어있어 문장이 카드 너비에 맞춰서 계속 줄바꿈돼서 가독성이 떨어져. 로그 형식으로 한줄씩 나오면 좋을듯.
- 해석: 현재 넓은 화면에서 2열 카드 격자라 카드 폭이 좁아 문장이 여러 번 줄바꿈된다. 카드 격자 대신 기록 한 건을 한 줄로 쌓는 로그 목록으로 바꿔 읽기 쉽게 한다.
- 코드 위치: `frontend/components/screens/DayScreen.tsx` — 단서 기록 대화상자의 `grid gap-3 sm:grid-cols-2` 안 `ObservationCard compact` 목록. 항목을 누르면 여는 원본 장면(`openObservation`) 상세는 별도.
- 관련: F4(플레이 안내·단서 기록 한 곳에)·F6(장면 그림을 단서 기록에 보관)과 같은 대화상자를 바꾸므로 함께 설계. 밤 화면 근거 목록(`NightScreen.tsx`)도 같은 `ObservationCard`를 쓰는지는 설계 때 확인.
- 조치: 반영 (2026-09-18 오후, 낮 화면 재구성 — 계획 `docs/superpowers/plans/2026-09-18-day-screen-vn.md`, 설계 `specs/2026-09-17-day-screen-vn-design.md`, 프론트는 Codex CLI) — 단서 기록을 카드 격자 대신 한 줄 로그(`RecordLog`: '1일째 · 장면 1 · 채연' 라벨 + 문장, 그림 있으면 '그림 보기').

### F8 — "오늘 남은 대화 N회"·"N일째"를 화면 상단 HUD에 체력 바처럼 표시 (2026-09-17)

- 장면: 낮(Day) — 입력창 위 "오늘 남은 대화 8회" 텍스트
- 요청:
  > "오늘 남은 대화 8회" 이것도 HUD에 health bar처럼 표시되면 좋을듯
- 추가 요청 (3일째 아침):
  > n일차인 것도 health bar과 함께 화면 상단에 잘 보이게 HUD로 표시됐으면 좋겠어
- 해석: 남은 대화 횟수를 입력부 위 문장 대신 화면 상단 HUD에 게이지(칸·바)로 보여줘 한눈에 소모량을 알 수 있게 한다. 같은 HUD에 "N일째 / 5일"을 함께 크게 표시한다.
- 일차 표시 현재 동작: 아침 화면은 본문 문장 "5일 중 {loopN}일째 아침이다."(`MorningScreen.tsx` 64행), 낮 화면은 입력창 위 작은 흐린 글씨 "{loopN}일째 · 하루 공용"(`DayScreen.tsx` 596행)뿐이고 밤·신의 개입 화면에는 일차 표시가 없다. HUD는 아침·낮·밤·신의 개입 모든 화면에서 같은 위치에 두는 편이 일관된다.
- 코드 위치: `frontend/components/screens/DayScreen.tsx` 입력부 상단 `오늘 남은 대화 {budgetLeft}회`, 아침 화면 `frontend/components/screens/MorningScreen.tsx`의 같은 문구. 최대치는 회차마다 다르다 — `backend/apps/engine/domain/entities/loop_rules.py` `initial_budget(loop_n) = 9 - loop_n`(1회차 8 → 5회차 4).
- 설계 시 확인: 게이지 최대치를 회차별 시작값으로 둘지(현재 API는 `budget_left`만 내려줌 — 시작값은 `loopN`으로 계산 가능), 스크린리더용 `aria-live` 텍스트 유지.
- 관련: F5·F6(낮 화면 재구성)과 같은 화면이라 HUD 위치를 함께 정한다.
- 조치: 반영 (2026-09-18 오후, 낮 화면 재구성 — 계획 `docs/superpowers/plans/2026-09-18-day-screen-vn.md`, 설계 `specs/2026-09-17-day-screen-vn-design.md`, 프론트는 Codex CLI) — 모든 화면 상단 HUD에 'N일째 / 5일'. 대화 횟수는 장면 제목 아래 '남은 대화 횟수' 칸 게이지(▮▮▮▯)로, 차감 시 칸이 꺼진다(사용자 요청으로 HUD 대신 제목 아래).

### F9 — 신의 개입 조언("내일 ○○에게 … 물어봐라")을 모아 보기, 또는 플레이어 메모장 (2026-09-17)

- 장면: 밤 — 신의 개입(신의 질문 답변)
- 요청:
  > 신의개입에서 "내일 준에게 배급이 어디서 오는지 물어봐라." 이런 식으로 뭐 하라고 추천해줄 때가 있는데 이것도 모아볼 수 있으면 좋겠어. 아니면 사용자가 직접 기록하도록 메모장 기능 같은 걸 제공해도 좋을 것 같아.
- 해석: 밤에 받은 "다음 날 할 일" 조언이 그 화면을 지나면 사라져, 다음 낮에 무엇을 물어야 하는지 기억에 의존해야 한다. 두 안 중 하나(또는 둘 다):
  - 안 A — 받은 조언을 자동으로 모아 낮 화면(예: 단서 기록 옆)에서 다시 볼 수 있게 한다.
  - 안 B — 플레이어가 직접 적는 메모장을 제공한다.
- 코드 위치: 조언 문장 생성 `backend/apps/engine/app/use_cases/advisor_advice.py`(`내일 {lead.target}에게 {lead.ask} 물어봐라.`), 표시 `frontend/components/screens/GodScreen.tsx`.
- 관련: F4(플레이 안내·단서 기록 한 곳에) — 모아 보기 위치를 같은 메뉴에 둘지 함께 정한다.
- 조치: 부분 반영 (2026-09-18) — 신의 조언은 기록 패널 '단서 기록'에 'N일째 · 신의 조언' 줄로 시간순 표시(별도 탭·메모장은 두지 않기로 설계 결정).

### F12 — 이 장면에서 대화를 마친 인물은 회색 처리 등으로 비활성 상태를 강조 (2026-09-17)

- 장면: 낮(Day) — 인물 선택 카드
- 요청:
  > 대화 완료된 캐릭터는 회색으로 처리하든지 해서 비활성화 상태를 강조하는 게 좋을 거 같아
- 현재 동작: 대화를 마친 인물 카드도 겉모습은 그대로이고, 카드 아래 작은 글자 "이 장면 대화 완료"로만 구분된다. 카드 자체는 선택 가능하며, 선택하면 입력창이 잠기고 안내 문구가 나온다(`selectedUttered`).
- 해석: `uttered` 인물 카드를 흑백·흐림 처리해 한눈에 비활성임을 보여준다. 선택까지 막을지(테스터7 F2의 "딤·선택 불가"와 같은 방식)는 설계 때 정한다.
- 코드 위치: `frontend/components/screens/DayScreen.tsx` 인물 선택 카드 `npcs.map` — `status`("이 장면 대화 완료")·버튼 `className`.
- 관련: 테스터7 F2(장면에 없는 NPC 딤·선택 불가)·F3(2×2 배열), 테스터9 F6(낮 화면 재구성). 딤 기준이 "장면 부재"와 "대화 완료" 두 가지이므로 표시를 구분할지 함께 정한다.
- 조치: 반영 (2026-09-17 밤, 프론트 커밋 `b05ffc6`) — 이 장면 대화를 마친 인물 카드를 흐림·흑백으로(선택은 막지 않음, `DayScreen.tsx`).

### F13 — 장면 설명과 규칙 실행 결과가 한 문단으로 합쳐져 채팅창에 쏟아짐 (2026-09-17)

- 장면: 2회차 낮 3번째 장면(정오 배급) 진입 직후 채팅창
- 요청:
  > 세번째 장면으로 넘어가니까 채팅창에 갑자기 "정오 배급이 나온다. 채연이 쟁반을 밀어낸다. 채연: 쟁반 밀었어. 지금은 안 먹고 싶어. 채연의 설명을 듣느라 다음에 말을 걸 여유가 줄었다. 민석이 배급 자리를 보고 수첩에 뭔가 적는다. 민석: 배급 자리 보고 적었어. 잊으면 안 되잖아. 민석의 설명을 듣느라 다음에 말을 걸 여유가 줄었다. 민석이 수첩을 펼쳐 보여준다. 날짜와 이름, 남은 쟁반 수가 줄지어 적혀 있다." 이런 엄청난 양의 텍스트가 뜨는데 이게 뭐지?

  > 이것도 UI를 좀 수정해야될거같아
- DB 확인(판 `e6290e17…`, 이벤트 12225~12235): 한 문단은 장면 기본 설명 + 이 장면에 걸린 규칙 3개의 실행 결과를 이어 붙인 것이다 — R1(1회차 원숭이손, 채연 설명)·R3(2회차 원숭이손, 민석 설명)·R2(1회차 밤 선택, 민석 수첩 보여주기). 원숭이손 대가 문구("설명을 듣느라 … 여유가 줄었다")가 두 번 들어 있고 실제로 대화 2회가 차감됐다. 같은 내용이 단서 기록에는 줄 단위(`scene`·`statement`·`rule_result`)로 따로 저장되어 있다.
- 해석: 무엇이 원래 장면이고 무엇이 규칙 결과·대가인지 구분이 안 되고, 대화 횟수가 왜 줄었는지도 문단 속에 묻힌다. 줄 단위로 나눠 보여주고, 규칙 결과·대가 문구는 시각적으로 구분(예: 규칙 표시·HUD 차감 연출)한다.
- 코드 위치: 서버가 `backend/apps/engine/app/use_cases/scene_execution.py`에서 장면 설명·규칙 결과·대가 문구를 `" ".join(narration)`으로 한 문자열로 합쳐 내려주고, `frontend/components/screens/DayScreen.tsx`가 `role: "narration"` 한 줄로 채팅 로그에 넣는다. 구분 표시를 하려면 응답에 줄 목록·종류를 함께 내려주는 백엔드 변경이 필요할 수 있다.
- 관련: F5(대사 줄 단위 넘기기)·F8(남은 대화 HUD — 차감 연출).
- 조치: 반영 (2026-09-18 오후, 낮 화면 재구성 — 계획 `docs/superpowers/plans/2026-09-18-day-screen-vn.md`, 설계 `specs/2026-09-17-day-screen-vn-design.md`, 프론트는 Codex CLI) — 서버가 종류가 붙은 줄 목록(`lines`: scene·action·rule_result·statement·broadcast·npc·paw_effect·fragment·system)을 보내고 대사창이 한 줄씩, 종류별 스타일(`lineStyles.ts`)로 보여 준다. 규칙 결과·원숭이손 부작용·방송이 서로 다른 모양.

### F15 — 장면 전환 대기 문구에 효과음 추가 (freesound.org) (2026-09-17)

- 장면: 낮(Day) — 다음 장면으로 넘어가는 동안 채팅창에 순환하는 대기 문구
- 요청:
  > '스피커가 지직거린다 복도 끝에서 발소리 형광등 깜빡' 이거는 freesound.org에서 효과음 구해서 넣으면 좋을 거 같아
- 현재 동작: 장면 경계에서 서버 응답을 기다리는 동안 `BEAT_WAIT_LINES` 4줄("스피커가 지직거린다…"·"복도 끝에서 발소리."·"형광등이 깜빡인다."·"누군가 자리를 옮긴다.")이 글자로만 순환한다. 로그에는 남지 않는다.
- 해석: 각 문구에 맞는 효과음(스피커 잡음·복도 발소리·형광등 깜빡임·자리 옮기는 소리)을 붙여 대기 시간을 연출한다.
- 코드 위치: `frontend/components/screens/DayScreen.tsx` `BEAT_WAIT_LINES`(55행)·순환 인덱스(190행)·표시(568행). 현재 음원은 `frontend/public/audio/`(BGM)·`audio/voice/`(음성)만 있고 효과음 폴더는 없다. 켜기/끄기는 `frontend/lib/audioSettings.ts`(BGM·음성 채널만 있음).
- 설계 시 확인: freesound.org 음원은 파일마다 라이선스가 다르다(CC0 / CC BY — 출처 표기 필요 / CC BY-NC — 비상업만). 제출·공개 서비스에 쓸 수 있는 라이선스만 고르고 출처 목록을 남긴다. 효과음을 BGM·음성과 같은 켜기/끄기로 묶을지 별도 채널로 둘지, 대기 시간이 짧을 때 소리가 잘리는 처리. 소리 담당은 팀 역할상 충식.
- 조치: 미착수 (기록만)

### F16 — 신의 질문 사용법(가설을 넣어 판정받는 곳)을 게임 안에 명시 (2026-09-17)

- 장면: 밤 — 신의 개입 질문 입력
- 요청:
  > 신의 질문은 원래 "맞다·아니다·알 수 없다를 판정받는 곳이라, 가설을 넣어 묻는 게 정상적인 사용법이에요." --> 이 점도 게임 내에 명시하는 게 좋을 거 같아
- 배경: 보조 플레이 중 Claude가 "인물에게는 가설을 넣은 유도 질문을 피하고, 신의 질문에는 가설을 넣어 판정받는다"고 구분해 안내한 내용을 게임 안내로 옮기자는 요청.
- 현재 동작: 입력창 위 안내는 "이 목소리는 오늘 일어난 일만 안다. 무엇을 했는지 물어라."와 작은 글씨 "맞다 · 아니다 · 그건 알 수 없다"뿐이다. 입력창 예시(placeholder)는 "내 주장을 확인할 기록은?"·"아직 모르는 이유를 어디서 확인해?"·"이 발언과 직접 본 사실은 어떻게 달라?" 등 찾기형 질문이라, 가설을 넣어 예/아니오로 묻는 사용법이 드러나지 않는다. 플레이 안내(`GameplayGuide`)에는 "3회 질문"만 있다.
- 정확성 메모: 신의 질문은 예/아니오 판정(proposition)과 찾기형(lookup, 누가·무엇·왜) 둘 다 받는다. "왜" 질문에는 "왜인지는 내가 말할 수 없다. 그 전에 일어난 일은 말할 수 있다."가 붙는다. 안내 문구는 "가설을 넣어 물으면 맞다·아니다로 판정받고, 누가·무엇을 물으면 기록에서 찾아 준다" 정도로 두 사용법을 함께 적는 편이 정확하다.
- 코드 위치: `frontend/components/screens/GodScreen.tsx` 안내 문구(236-237행)·`questionPlaceholders`(97-101행), `frontend/components/GameplayGuide.tsx`.
- 관련: 백엔드/AI F10(예/아니오 질문이 찾기형으로 잘못 분류돼 "맞다" 오판정) — 안내에서 예/아니오 사용을 권하면 F10 수정이 먼저 필요하다. F11 유도 질문 항목(인물에게는 가설을 넣지 않기)과 짝으로 안내한다.
- 조치: 반영 (2026-09-18, 계획 `docs/superpowers/plans/2026-09-18-god-question-improvements.md` §3, 프론트 커밋 `b05ffc6`) — 입력창 위 안내 "가설을 넣어 물으면 맞다·아니다로 판정받고, 누가·무엇을 물으면 기록에서 찾아 준다.", 자리표시자에 최근 추리문 예시. 게임 목적·사용법 질문은 모델 호출 없이 고정 안내로 답한다.

### F18 — 새로 나온 단서에 "NEW" 표시·모아 보기, 신의 질문 화면에서 이전 단서 기록 열람 (2026-09-17)

- 장면: 밤 — 제출 뒤 결말 연출·신의 개입 화면
- 요청:
  > 이번 밤에 새로 나온 단서 --> 이것도 약간 new! 이런 식으로 알기 쉽게 표시해서 모아볼 수 있으면 좋겠어. 그리고 신의질문 화면에서도 이전 단서 기록을 볼 수 있으면 좋겠어
- 배경: 보조 플레이 중 Claude가 DB에서 찾아 "이번 밤에 새로 나온 단서"로 따로 알려 준 항목(2회차 밤 "트럭 소리."·"정리 작업이 있겠습니다. 비어 있는 자리는 아침에 정돈됩니다.")을 플레이어가 게임 안에서 스스로 알아볼 수 있게 하자는 요청.
- 현재 동작: 밤 단서는 결말 연출(치지직 띠)에서 잠깐 지나가고 이후 단서 기록 목록에 다른 기록과 섞인다. 새 단서를 구분하는 표시가 없다. 신의 개입 화면(`GodScreen.tsx`)은 질문 답변에 붙은 근거 카드(최대 2개)만 보여주고, 단서 기록 전체를 여는 진입점이 없다.
- 해석:
  1. 직전 밤·오늘 처음 드러난 단서에 "NEW" 배지를 붙이고, 새 단서만 모아 보는 필터(또는 목록 상단 묶음)를 둔다.
  2. 신의 질문 화면에서도 단서 기록을 열어 이전 기록을 보며 질문을 쓸 수 있게 한다.
- 코드 위치: 신의 개입 `frontend/components/screens/GodScreen.tsx`(근거 카드 223행), 결말 연출 `frontend/components/screens/DoomTransition.tsx`, 단서 기록 대화상자 `frontend/components/screens/DayScreen.tsx`. "새 단서" 판정은 관찰의 `loop_n`·`beat`·`observation_id`(예: `night-clue`·`outcome-fragment-*`)로 가능해 보이나, 플레이어가 이미 본 것인지(읽음 여부)는 현재 저장하지 않는다.
- 관련: F4(플레이 안내·단서 기록 한 곳에 — 모든 화면 공통 진입점), F7(단서 기록 로그 형식), F9(조언 모아 보기), 테스터6 F6(이전 회차 중복 기록 제외).
- 조치: 반영 (2026-09-18) — 1: 기록 패널 NEW 배지 + "새 단서만" 필터(현재 회차 관찰 + 직전 회차 night-clue/outcome-fragment, 재개방 시 해제, 빈 상태 "아직 새 단서가 없다."). 2: 신의 개입 화면 HUD [기록] 버튼으로 기록 열람(낮 화면 재구성 T4).

### F20 — 게임 전반에 덩어리 텍스트·줄글이 많아 머리에 안 들어옴 (상위 원칙) (2026-09-17)

- 장면: 게임 전체
- 요청:
  > 지금 게임 전반적으로 덩어리 텍스트/줄글이 너무 많아서 머리에 잘 안들어오는 경향이 있는데 이건 어떻게 수정하면 좋을까
- 이번 판에서 확인한 덩어리 지점:

  | 화면 | 사례 | 문제 |
  |---|---|---|
  | 낮 장면 전환 | 장면 설명 + 규칙 결과 3개 + 대가 문구가 한 문단(F13) | 종류가 다른 정보가 한 덩어리 |
  | 인물 대사 | 채팅 로그에 누적(F5) | 지금 읽을 줄이 흐려짐 |
  | 신의 질문 답 | 판정 + 본문 최대 3문장 + "네 기록의 「…」. 내일 ○○에게 … 물어봐라." + 근거 카드 | 답·조언·인용이 섞임 |
  | 규칙 고르기 | 선택지마다 "이유 · (NPC 대사 전문)" + "확인 · 다음 하루에 민석의 알고 있는 관찰을 설명한다 행동을 확인한다." | 기계적 긴 문장 3회 반복 |
  | 플레이 안내 | 규칙 8개 긴 문장 나열(`GameplayGuide`) | 한 번에 다 읽어야 함 |
  | 밤 추리문 | 이전 밤 글이 이어져 회차마다 길어짐 — 이 판 2회차 제출문(이벤트 12424)에 1·2회차 글이 모두 들어 있음 | 자기 글도 다시 읽기 어려움 |

- 수정 원칙:
  1. 한 번에 한 줄 — 낮 대사·장면 설명은 대사창에서 한 줄씩(F5), 서버는 문단 대신 줄 목록으로 전달(F13).
  2. 종류마다 모양을 다르게 — 인물 대사·방송·규칙 결과·대가를 글꼴·색·아이콘으로 구분. 대가 문장은 HUD 게이지 차감 연출로 대체(F8).
  3. 문장 대신 표시 — 판정 배지(맞다/아니다/?), NEW 배지(F18), 남은 대화 게이지(F8), 대화 완료 회색 처리(F12).
  4. 요약 먼저, 자세히는 펼쳐서 — 신의 질문은 "판정 + 한 줄"만 먼저, 근거·조언은 접기. 규칙 선택지는 제목만 먼저, 이유는 펼치기. 반복되는 "확인 · 다음 하루에 … 확인한다" 문장 제거.
  5. 시스템 문구 짧게 재작성 — 플레이 안내는 핵심 3줄 + 처음 겪는 순간의 한 줄 팁. 조립형 문장("~행동을 확인한다")은 사람이 쓴 짧은 문구로.
- 우선순위: 효과 최대는 1·2(낮 화면 비주얼노벨 전환 F5·F6·F13 묶음). 구조 변경 없이 바로 가능한 것은 4·5(신의 질문 답·규칙 선택지 접기와 문구 축소).
- 코드 위치: `frontend/components/screens/DayScreen.tsx`·`GodScreen.tsx`(답 로그 205-232행, 선택지 "이유 ·"/"확인 ·" 333-334행)·`NightScreen.tsx`, `frontend/components/GameplayGuide.tsx`, 서버 문단 합치기 `backend/apps/engine/app/use_cases/scene_execution.py`, 선택지 문구 `expected_observation` 생성(`intervention_interactor`).
- 관련(하위 항목): F4·F5·F6·F7·F8·F9·F12·F13·F16·F18, 백엔드/AI F19.
- 조치: 반영 (2026-09-18 오후, 낮 화면 재구성 — 계획 `docs/superpowers/plans/2026-09-18-day-screen-vn.md`, 설계 `specs/2026-09-17-day-screen-vn-design.md`, 프론트는 Codex CLI) — 덩어리 텍스트를 줄 단위로 나눠 한 줄씩 넘기게 하고(대사창), 안내는 4줄로 축약, 기록은 한 줄 로그. 조립형 내부 문구 정리는 계속 과제.

### F21 — 규칙이 이후 모든 날에 누적 적용되고 원숭이손 대가도 매일 반복된다는 사실이 안내되지 않음 (2026-09-17)

- 장면: 3일째 아침 — 낮 계획 중
- 요청:
  > 한번 건 규칙은 그 이후 모든 날짜에 다 적용되는거야?
- 확인(코드·DB):
  - 누적 적용 — 매일 새 날 시작 때 그 판의 규칙 전부(`self._rules.list(attempt_id)`, `backend/apps/engine/app/use_cases/loop_interactor.py` 152행)를 인물 계획에 적용한다. 만료·하루 한정 처리는 없다. 반대 효과(enforce/suppress) 규칙이 같은 인물·장면·행동에 겹치면 나중 규칙이 이기고 세계 손상 +1(`backend/apps/engine/domain/entities/rule_rules.py` `find_conflicts`·`enforce_rules_on_plan`).
  - 실제 사례 — 1일째 원숭이손 R1(채연 설명)이 2일째 3번째 장면에서도 실행되어 대가 문구와 대화 차감이 발생했다(이벤트 12227·12228).
  - 이 판 3일째 기준 걸린 규칙: R1 채연 설명(3번째, 원숭이손·매일 −1) · R2 민석 수첩 보여주기(3번째) · R3 민석 설명(3번째, 원숭이손·매일 −1) · R4 준 밤에 깨어 있음(6번째).
  - 영향 — 원숭이손 대가가 매일 반복되어 3~5일째 3번째 장면에서 하루 2회씩 차감된다. 기본 대화 6/5/4회 중 5일째는 실사용 2회.
- 현재 안내: 플레이 안내(`frontend/components/GameplayGuide.tsx`)에는 "특별한 규칙의 대가로 그날의 대화 횟수가 더 줄어들 수 있다."뿐이다. 규칙 유지·대가 반복은 적혀 있지 않고, "그날의"라는 표현은 하루만 적용되는 것처럼 읽힌다. 원숭이손 제안 문구(`reason_shown`)도 "다음에 직접 한 일을 말로 설명한다. 놓친 관찰을 들을 수 있다."로 대가·지속 기간을 보여주지 않는다(대가는 `hidden_side_effect`로 숨김).
- 해석: 플레이어가 규칙을 고르거나 원숭이손을 받아들일 때 "이후 모든 날에 적용"과 "대가가 매일 반복"을 알 수 있어야 한다. 대화 횟수가 왜 줄었는지 모르는 문제(F13)와 이어진다.
- 개선 방향: ① 플레이 안내·규칙 선택·원숭이손 제안 화면에 "이 규칙은 남은 모든 날에 적용된다" 명시 ② 현재 걸린 규칙 목록을 HUD/메뉴(F4·F8)에서 상시 확인 ③ 원숭이손 대가를 숨길지 공개할지는 설계 의도(숨은 대가가 원숭이손 콘셉트) 확인 후 결정 — 숨기더라도 발생 시 HUD 차감 연출로 원인 표시.
- 관련: F4(공통 메뉴)·F8(HUD 차감)·F13(대가 문구 묻힘)·F20(문구 원칙), 테스터6 F11(원숭이손 부작용 불분명), F11 난이도 조정(누적 대가가 후반 난이도를 올림).
- 조치: 반영 (2026-09-17 밤 안내 묶음, 프론트 커밋 `b05ffc6`) — 플레이 안내(`GameplayGuide.tsx`) "특별한 규칙을 받아들이면 그날부터 남은 모든 날에 계속 걸린다", 규칙 선택 화면(`GodScreen.tsx`) "남은 모든 날에 규칙 하나를 건다." 원숭이손 대가의 매일 반복 안내는 소원 4종 재설계(`2026-09-16-f11-monkey-paw-wishes.md`) 문장에 포함.

### F22 — "다음" 등 버튼이 "…"로만 표시되는 경우가 많음 (2026-09-17)

- 장면: 3일째 낮 — 장면 넘기기 등
- 요청:
  > "다음" 버튼이어야 할 것들이 "..."라는 버튼으로 표시되는 경우가 많은데 이것도 수정해야할듯
- 확인(코드): 처리 중(`busy`)일 때 버튼 라벨 전체를 "…"로 바꾸는 패턴이 여러 화면에 있다 — 낮 "다음 장면"(`DayScreen.tsx` 690행 `beatAction.busy ? "…" : "다음 장면"`), 결말 "다시"(`DoomEndScreen.tsx` 31행), 클리어 "다시 시작"(`ClearScreen.tsx` 44행), 규칙 "해석 미리보기"(`GodScreen.tsx` 365행), 주장 수정 "이렇게 고친다"(`ConfirmScreen.tsx` 145행). 낮 입력창 placeholder(`DayScreen.tsx` 658행)·근거 목록 로딩(`NightScreen.tsx` 182행)도 "…".
- 확인(DB, 이 판 3일째): 장면 넘김 응답 대기가 2→3번째 장면 약 8초, 3→4번째 장면 약 19초(첫 관찰 기록 시각 13:06:07 → 13:06:15 → 13:06:34). 이 동안 "다음 장면" 버튼이 "…"로 보여 버튼 정체·진행 상태를 알기 어렵다.
- 해석: 처리 중에도 원래 라벨을 유지하고 진행 표시(예: "다음 장면 준비 중…", 스피너·진행 점)를 덧붙인다. 대기 시간이 긴 장면 넘김은 F15(대기 문구·효과음) 연출과 함께 설계한다.
- 관련: F15(장면 전환 대기 연출), F20(문구 원칙). 대기 시간 자체(관리자 검사·계획 조립 LLM 호출)는 백엔드 성능 문제로 별도.
- 조치: 반영 (2026-09-17 밤, 프론트 커밋 `b05ffc6`) — 버튼의 "…"를 "다음 장면 준비 중…" 등 문구와 `aria-busy`로(`DayScreen`·`GodScreen`·`ClearScreen`·`DoomEndScreen`·`ConfirmScreen`). 관리자 방송 팝업 확인 버튼 라벨은 HANDOFF 4번 결정 대기.

### F25 — 클리어 후 "개발 데이터"에 내부 ID(uuid)가 그대로 노출됨 (2026-09-17)

- 장면: 클리어 화면 "이 세계의 바깥으로" → 회고 → "개발 데이터 — 규칙·검사·모델 동작이 궁금하다면" 펼침
- 요청:
  > '이 세계의 바깥으로'를 누른 다음 개발 데이터를 열람하면 uuid가 그대로 노출되어있는데 이런것들도 좀 수정해야 할 거 같아
- 확인(코드): 실험 기록의 기회 카드(`OpportunityCard`)가 "관찰 근거" 칸에 `opportunity.observation_ids.join(", ")`를 그대로 출력한다(`frontend/components/Retrospective.tsx` 57행). 관찰 ID 형식은 `{loop uuid}:action-2-준-손목띠를 만진다`·`{loop uuid}:rule-3-R1`·`{loop uuid}:paw-cost-3-R1`이라 회차(loop) uuid와 내부 키가 그대로 보인다.
- 해석: 플레이어용 화면에 내부 식별자를 보여줄 이유가 없다. ID 대신 해당 관찰의 문장(또는 "2회차 · 장면 3 · 준의 행동"처럼 사람이 읽는 라벨)으로 바꾸고, 연결 기록이 여러 개면 개수·접기로 정리한다.
- 함께 볼 것:
  - 같은 섹션의 "의도" 칸(`experiment.intent`)도 원문을 그대로 보여준다. 이 판 R2의 `intent`에는 규칙과 무관한 1회차 추리문 전문이 저장돼 있었다(이벤트 12197 `rule_applied.intent`). 표시 문제와 별개로 저장 값 자체 점검 필요.
  - 노출된 회차·판 uuid로 다른 API(`/loops/{id}/…`·`/nights/{id}/…`)에 접근할 때 소유자 확인이 있는지는 이번에 확인하지 않았다. 보안 영향이 있으면 A등급으로 별도 점검.
- 관련: F20(문구 원칙 — 조립형·내부 문구 제거).
- 조치: 반영 (2026-09-17 밤, 프론트 커밋 `b05ffc6`) — 회고의 관찰 근거를 uuid 대신 "장면 3 · 준의 행동: …" 라벨로 바꾸고 3건 이상이면 접음(신규 `frontend/lib/observationLabel.ts`, `Retrospective.tsx`). `intent` 저장 값 점검 완료(2026-09-18) — DB 표본(포트 5435, `rule_applied` 167건) 확인 결과 이벤트 12197을 포함해 09-09~09-17 기록된 추천 규칙(`user_choice`) intent 전체가 옵션 라벨이 아니라 그날 밤 추리문 전문이었다(uuid는 아니고 잘못된 값). 원인은 당시 코드가 intent를 옵션 라벨과 다른 값으로 채우던 것; `intent`가 아예 없는 09-08 이전 기록은 필드 신설 전 레거시라 별도. 백엔드 커밋 `cf5a0b7`(2026-09-18 새벽, 본 브랜치 HEAD에 이미 포함)에서 `rule_intent()`/`_INTENT_BY_SOURCE`로 출처별 분리(추천 규칙=선택지 라벨, 직접 규칙=원문 그대로, 원숭이손=미기록)해 해결 — 관련 테스트 3건(`test_recommended_rule_intent_is_the_option_label_not_the_night_claims` 등) 통과 확인. 수정 이후 플레이가 아직 없어 DB에 정상값 표본은 없고 코드·테스트로 대체 확인. `monkey_paw`(원숭이손) 규칙은 intent를 아예 채우지 않아 회고에 "기록 없음"으로 뜨는데, 이는 내부 식별자 노출이 아니라 의도된 공백이라 추가 조치 불요. 판 API 소유자 확인은 F26으로 처리(반영됨).

## 백엔드/AI

### F2 — 개발 로그인 후에도 로그인 안내 화면만 반복됨 (2026-09-17)

- 장면: 랜딩 개발 로그인 → `/play`
- 요청:
  > 개발로그인이 안되고 계속 아까 그 문구만 떠
- 확인(서버 측): `backend.log`에서 `POST /api/v1/auth/dev/login` 200 직후 `POST /sessions` 401이 2회 반복. curl 재현 시 로그인은 200이고, 발급 쿠키를 직접 실으면 `/api/v1/auth/me` 200 — 세션 자체는 유효하고 브라우저가 쿠키를 싣지 않는 상태.
- 원인 후보: `backend/.env` `FRONTEND_BASE_URL=https://remakeday.com`(09-17 01:03 배포 준비로 변경) → `_cookie_secure()`가 true라 `rd_session`에 `Secure`가 붙음. Safari는 http localhost에서 Secure 쿠키를 거부하고, Chrome이라도 `127.0.0.1:3500`으로 접속하면 API(`localhost:8500`)와 호스트가 달라 쿠키가 실리지 않는다. 접속 브라우저·주소 미확인.
- 조치: 설정 변경 없이 해소 — 사용자가 이후 개발 로그인·접속 성공을 알림(2026-09-17). 어떤 브라우저·주소 변경으로 풀렸는지는 미확인이라 원인 후보는 확정하지 않는다. 09-20 제출 전 로컬 테스트 안내에 "Chrome + `http://localhost:3500`" 명시 여부 검토.

### F10 — 신의 질문 판정 "맞다"와 본문 "알 수 없다"가 모순 (2026-09-17)

- 장면: 밤 — 신의 개입, 1회차 두 번째 질문
- 요청:
  > 내가 지금 신의개입에다가 "민석의 보고가 채연의 이송으로 이어졌는가?"라고 물어봤는데 신의개입에서 '맞다'라고 뜨고 '민석은 채연이 남긴 배급의 양과 이름을 기록했다. 이 기록이 실제 이송으로 이어졌는지는 알 수 없다.' 이렇게 뜨는데 그럼 맞는 게 아니잖아;; 이것도 좀 수정해야할듯
- DB 확인(판 `e6290e17-8f36-4d3c-b134-14fb892d5016`, 이벤트 12194·12195): 조언자 모델(gemma4:12b) 출력이 `question_kind: "lookup"`, 근거 2건(민석이 수첩에 적는 관찰·민석의 전언) 모두 `relation: "supported"`. 최종 `intervention_question.status = "supported"` → 답변 앞에 "맞다."가 붙음.
- 원인 후보: ① 예/아니오 질문("…이어졌는가?")인데 모델이 `lookup`으로 분류(프롬프트 규칙은 예/아니오=proposition). ② 근거의 `supported`가 "질문 명제(보고→이송)"가 아니라 "기록했다는 하위 사실"을 뒷받침한다는 뜻으로 쓰였는데, 판정은 근거 관계가 하나로 모이면 그대로 status로 쓴다. ③ `intervention_interactor.py`는 `lookup`일 때 `contradicted`만 `unknown`으로 낮추고 `supported`는 그대로 둔다. 본문이 "알 수 없다"로 끝나도 판정과 대조하는 검사가 없다.
- 코드 위치: `backend/apps/engine/app/use_cases/intervention_interactor.py`(status 결정 ~310-320행), 판정 접두어 `backend/apps/engine/app/use_cases/advisor_advice.py` `verdict_prefix`.
- 관련: 테스터6 F7·F9(신의 질문 답변의 맥락·개연성).
- 재발 사례(2회차 밤): `밤에 트럭이 온 날 아침에는 비어 있는 자리가 생기는가?` → status `supported`, 답 "맞다. 밤에 트럭이 온다는 사실과 아침에 자리를 정돈한다는 방송 내용이 연결된다. 트럭의 방문이 비어 있는 자리의 발생이나 이동과 관련될 가능성이 있다." — 본문은 "가능성"인데 판정은 "맞다". 같은 계열 오판정.
- 조치: 반영 (2026-09-17 15:04 코드, 백엔드 커밋 `cf5a0b7`, 서버 반영 2026-09-18 07:24 재기동). 09-18 신의 질문 개선 스모크에서 헤지 어휘 누락("확실치 않다"인데 "맞다.") 1건을 추가로 잡아 `_HEDGE_CUES`에 "확실치 않"·"확실하지 않"을 더했다.
  - `backend/apps/engine/app/use_cases/advisor_advice.py`: `is_hedged`(본문에 "알 수 없"·"가능성"·"확인이 필요"·"확인되지 않"·"모른다"·"모르겠"·"불분명"·"단정할 수 없"), `is_wh_question`(무엇·뭐·누구·누가·어디·언제·무슨·몇·어느·어떤, "언제나·누구든" 제외), `settle_status`(본문이 확답을 피하면 supported/contradicted → unknown, lookup·wh 질문의 contradicted → unknown) 추가. `verdict_prefix`에 `wh` 인자 — wh 질문이 supported면 접두어 없이 본문만.
  - `intervention_interactor.py`: 기존 lookup 한 줄을 `settle_status` 호출로 교체, `verdict_prefix`에 `wh` 전달(메타 질문 제외). unknown이 되면 기존 분기대로 확인 노트 미생성·"확인된 기록:" 상세.
  - 테스트: 이 판 실제 사례 4건(3·4번째는 4일째 밤 "○○…은 무엇을 하는 곳인가"·"가축으로 관리되는가") 재현 + 확답 supported "맞다."/contradicted "아니다." 회귀 + wh 질문 동작 + 순수 함수 단위 테스트. `tests/engine/test_usecase_intervention.py`·`test_advisor_advice.py` 44 passed(컨트롤러 재실행 확인). 백엔드 전체 604 passed / 1 failed — 실패 `test_guards.py::test_user_daily_limit_is_five`는 수정 전에도 실패(무관).
  - 남은 위험: 핵심은 확답하고 곁가지만 "알 수 없다"인 답도 unknown으로 낮아져 확인 노트가 사라질 수 있음. "어디 아파?" 같은 표현이 wh로 잡혀 접두어가 빠질 수 있음. 실제 모델(gemma4:12b) 재확인 전.
  - 후속 제안(미적용): 조언자 프롬프트에 "relation은 질문 전체에 대한 관계로 평가, 불확실하면 unknown" 강화.

### F11 — 추천 질문으로 플레이한 1회차가 팀 휴먼테스트 최고 수준(55.4) (2026-09-17)

- 장면: 1회차 밤 — 채점 결과
- 요청:
  > 너가 추천해준 대로 플레이했더니 1회차 점수가 55%로 거의 우리 팀원들 휴먼테스트 중에 최고기록인데, 이 게임을 잘하는 팁이 뭐야?
- 플레이 조건: 낮 질문·밤 추리문·근거 선택·신의 질문을 Claude가 **공개 관찰만으로** 추천하고 테스터9가 그대로 입력(숨은 정답 미사용). 따라서 이 판은 "사람이 스스로 추리한 표본"이 아니라 "관찰 기반 보조 플레이" 표본이다.
- DB 확인(이벤트 12185 `answer_scored`): 총점 55.4·통과, cause 75.0·motive 83.3·identity 0·side_effect 0. 확정 5건 중 4건(cause-1·2·3, motive-1)이 같은 한 문장 "채연은 며칠째 배급을 남기며 몸 상태를 숨기고 있다."에 매칭, motive-2는 방송 원문 조각 "오후 검진을 시작합니다."에 매칭.
- 재검토(2026-09-17, 최초 기록의 "채점 관대함" 판단 정정):
  - 이 판만의 현상이 아니다. 같은 채점기로 테스터6 3회차는 확정 8건이 4문장, 테스터7 5회차는 확정 8건이 5문장에서 나왔다. 진실 명제가 "아프다"·"숨겼다"처럼 잘게 나뉘어 있어 한 문장이 여러 명제를 맞히는 것은 설계상 가능하다.
  - `cited_chunks`가 모든 명제에 똑같이 붙는 것은 채점 후보 전체를 기록하는 방식(`night_interactor._judge`의 `cited: list(user_claims)`)이고, 탭한 근거 원문이 후보에 들어가는 것도 `source_claims()` 설계다. 관대함의 증거가 아니다.
  - 실제 문제 1 — 매칭 문장 지목 오류: motive-2(이송 규정)는 제출문 "검진 방송에 따르면 상태가 나쁜 사람은 별도 구역으로 이송된다."가 맞는 짝인데 기록은 방송 조각을 가리킨다. 판정은 타당하나 "내 주장" 표시가 틀린다.
  - 판단 경계 — cause-1(아프다)·cause-3(검진 회피)·motive-1(이송이 두려워 숨김)은 "몸 상태를 숨긴다" 한 문장에서 추론으로 인정됐다. 엄격도 정책 문제이지 이 판에서만 느슨해진 것은 아니다.
  - 결론: 55.4는 주로 추리문이 핵심 가설(몸 상태를 숨김 → 검진·이송 규정)을 실제로 서술했기 때문이다. 보조 플레이의 행동 팁은 난이도 조정 근거로 유효하다.
- 재정정 (3일째 밤, 2026-09-17): 위 재검토의 "관대함의 증거가 아니다"는 절반만 맞았다. **실제 오인정 사례가 있다.**
  - 2일째(이벤트 12438, 64.2): cause-5(관리자 쪽 조치 명제)가 `confirmed`, 매칭 문장은 이어받은 근거 원문 조각 "오후 검진을 시작합니다."다. 명제 내용과 무관한 방송 조각으로 인정됐다.
  - 3일째(이벤트 12939, 55.4): 추리문을 새로 쓰고 주장 목록을 새 글 6문장만 남기자(이벤트 12928, 근거 원문 조각이 후보에서 빠짐) cause-5가 `none`으로 돌아가 cause 100→75, 총점 64.2→55.4. 새로 쓰라는 안내는 Claude 추천이었다.
  - 매칭 문장 지목 오류도 재발: 3일째 motive-2(이송 규정)의 매칭 문장이 "검진 방송에 따르면 … 이송된다."가 아니라 "검진 때도 담요를 끌어올린 채 기다리며 몸 상태를 드러내지 않는다."로 기록됐다.
  - 수정된 결론: 한 문장의 다중 명제 매칭은 설계상 정상이나, 근거 원문 조각(특히 방송 원문)이 무관한 명제에 인정되는 오인정이 실제로 있다. 채점 엄격도 검토 범위에 "근거 원문 조각 매칭"을 포함한다(F17과 직결).
- 추가 요청:
  > 팁으로 공유하려는 게 아니라 지금 다른 팀원들 피드백에서 공통적으로 나오고 있는 게 개발자인 우리에게도 너무 어려운 게임이라는거야. 그 '팁'을 참고해서 좀 조정할 필요가 있어보여
- 해석: 팀 공통 피드백이 "개발자에게도 너무 어렵다"(테스터8 F3·F4 등)이므로, 보조 플레이에서 효과가 있던 행동(본 것 기반 구체 질문, 반복 답변 인물 건너뛰기, 대사와 방송 규칙 잇기, 사실 하나씩 인과로 잇는 추리문, 새 단서에 신의 질문 쓰기)을 게임 안의 안내·보조 장치로 옮겨 난이도를 조정한다.
- 1회차 점수 비교(사람 표본): 테스터6 8.8 · 테스터7 33.5 · 테스터8 0.0 · 테스터9 55.4(보조 플레이).
- 추가 확인 (유도 질문):
  > 지금 너의 패턴을 보면 '어디 아픈 거 들킬까 봐 그래?'라든지 '이송되는 거?' 이런 식으로 캐릭터에게서 특정 답을 유도하는 듯이 물어보는 경향이 있는데 이것도 게임을 잘하는 팁이야?
  - 결론: 팁이 아니다. 보조 플레이 추천에 가설을 질문에 넣는 유도 질문이 섞였으나, 55.4는 밤 추리문(공개 관찰을 이은 가설)에서 나왔고 유도 질문으로 얻은 확인은 없었다(1회차 "진짜 배가 안 고팠어?" → "응, 조금 먹었어"로 새 정보 없음).
  - 역효과 가능성: NPC(kanana1.5:8b)가 질문 속 가설을 되받아 말할 위험이 있고(F14처럼 프롬프트 규칙 이탈 사례 존재), NPC 발언은 원래 `reported`(전언)라 관찰보다 약하다. 유도해 얻은 전언은 플레이어가 심은 정보와 구분되지 않아 표본도 오염된다.
  - 난이도 조정 반영 시: "본 것에서 출발해 구체적으로 묻기"는 **본 행동을 짚어 묻기**("검진 기다릴 때 어땠어?")까지로 한정한다. 추천 질문 버튼 등 보조 장치를 만들 때 질문에 가설·정답 방향을 넣지 않는다.
  - 후속 확인 거리: NPC가 유도 질문의 가설을 되받아 확인해 주는지(피암시성) 별도 점검.
  - 사례(2회차 4번째 장면, 정정 전 유도 질문이 그대로 입력됨): 은상 `뭐가 무서운데? 이송되는 거?` → `응. 나도 아프면 다른 데로 가야 할까 봐 무서워.`(이벤트 12332, "응"으로 가설 수용). 채연 `…어디 아픈 거 들킬까 봐 그래?` → `조금 추운 것 같아서 그랬어. 근데 혹시라도 이상한 거 보일까봐 신경 쓰였어.`(이벤트 12352, 일부 수용). 피암시성 점검의 첫 사례로 쓴다.
- 주의: 채점 엄격도(cause-1·cause-3·motive-1 같은 추론 인정 범위)를 조이면 같은 플레이의 점수가 내려가 체감 난이도가 더 오른다. 엄격도 조정 여부와 난이도 보조를 같은 계획에서 목표 점수대를 정해 함께 다룬다. 매칭 문장 지목 오류는 별개 버그로 수정 대상.
- 조치: 부분 반영 (2026-09-18) — **매칭 문장 지목 오류**: 원인은 채점 모델이 `why`에서 맞는 후보를 인용하면서 번호를 하나 낮게 내는 것(실판 181건 중 38건). 인용(`matched_quote`)으로 번호를 보정해 재판정 249건 기준 번호 오류 35% → 0.6%(`model_evaluation.md` A.22, 계획 `2026-09-18-f11-matched-quote.md`, 서버 반영 14:32). **근거 원문 조각 오인정**은 F17 안 B(노트 비채점)로 해소. **채점 엄격도는 그대로 둔다**(사용자 결정 2026-09-18 — 난이도는 F19 환급·사다리 등 보조 장치로만 조정). 목표 점수대는 미정. 보조 장치 중 F16 안내·F19 환급·사다리는 반영.

### F14 — 민석 수첩: 내용이 비어 있고, 질문에 엇나간 답을 함 (2026-09-17)

- 장면: 2회차 낮 3번째 장면(정오 배급) — R2 규칙으로 민석이 수첩을 보여준 직후
- 요청:
  > 민석이가 "오늘은 채연 것만 봤어. 다른 사람은 안 봤어."라는데 이게 뭔소리야

  > "오늘 수첩에 적은 사람은 채연 한 명뿐"이라는 뜻인데 왜 그걸 "오늘은 채연 것만 봤어"라고 말해?
- 질문: `수첩에 이름 몇 명 적혀 있어? 채연 말고 또 누가 있어?` → 민석 답 `오늘은 채연 것만 봤어. 다른 사람은 안 봤어.` (이벤트 12250, NPC 호출 12248, kanana1.5:8b)
- DB 확인(NPC 프롬프트): 민석의 수첩 관련 기억은 m9 "채연이가 남긴 배급을 보고 이름과 남은 몫을 수첩에 적었어"와 m11 "수첩을 펼쳐서 적은 거 보여줬어. 날짜와 이름, 남은 쟁반 수가 적혀 있어"뿐이다. 적힌 이름·인원·날짜 범위는 어디에도 없다. 반면 "채연이 음식을 남기는 걸 봤다"는 기억은 m2·m7 두 건이다.
- 문제 1 (시나리오 내용 공백): 장면 설명은 "날짜와 이름, 남은 쟁반 수가 줄지어 적혀 있다"로 여러 날·여러 이름이 있는 것처럼 보여주는데, 수첩 내용이 정의되어 있지 않아 어떤 질문에도 답이 나올 수 없다. 단서가 기대만 만들고 끊긴다.
- 문제 2 (NPC 답변 품질): 질문은 "수첩에 몇 명·누가"인데 답은 "내가 오늘 본 것"으로 대상을 바꿨다. 프롬프트의 "질문한 대상을 먼저 짚어 줘"·"모르는 부분만 '그건 나도 몰라'처럼 표현" 규칙대로면 "수첩엔 채연 이름을 적었어. 몇 명인지는 잘 몰라" 같은 답이어야 한다. 모르는 질문을 가장 가까운 기억으로 둘러대는 패턴.
- 코드 위치: 수첩 문구 `backend/apps/scenarios/scenario_a/adapter.py` 286-287행(`narration`·`explanation`), NPC 기억 조립 `backend/apps/engine/app/use_cases/npc_context.py`.
- 추가 사례 1 — 기억과 반대되는 부정(3일째 6번째 장면, 이벤트 12917): `오늘 방송실에 가서 뭐 알렸어?` → 민석 `나는 오늘 방송실에 가지 않았어.` 민석 기억에는 5번째 장면 "방송실 문 앞까지 갔다 왔어. 이상한 건 알리랬잖아."(m18)가 있다. "안에 들어가 알리지는 않았다"로 읽을 여지는 있으나, 플레이어에게는 관찰("민석이 방송실 문 앞까지 갔다가 돌아온다")과 모순된 거짓말로 보인다. 의심·신뢰 변화 0, 성격 설정상 속일 동기 없음.
- 추가 사례 2 — 설명 규칙이 장면 행동을 되풀이할 뿐(4일째 2번째 장면, 이벤트 13324·13325): 밤에 고른 R5 `준: 알고 있는 관찰을 설명한다`가 실행됐으나 설명은 "불빛에 비춰 봤어. 숫자가 써 있잖아. 뭔지 궁금해서."로 화면 장면("준이 손목띠를 불빛에 비춰 본다")을 말로 반복했다. 준의 시작 기억(`jun-before-start-band`, 숫자 밑 작은 글자)은 설명에 쓰이지 않았다. 규칙 선택의 대가(밤 선택 1회)에 비해 얻는 정보가 없다. 선택 당시 Claude 추천이었고, 2일째 밤 조언은 `준: 가진 것을 보여준다`였다.
- 추가 사례 3 — 설정에 없는 사실 지어냄(4일째 2번째 장면, 이벤트 13356): `네 손목띠 숫자 읽어 줄래? 다른 사람 띠랑 같아?` → 준 `내 거는 7이야. 채연이는 5, 민석이는 3이라고 했어. 다 달라.` 준의 기억·오늘 대화·시나리오 어디에도 손목띠 숫자 값이나 채연·민석이 숫자를 말한 기록이 없다. 프롬프트의 "서로 다른 관찰을 섞거나 새로운 … 덧붙이지 마" 규칙 위반. 플레이어가 단서로 오인할 위험이 크다.
- 참고: Claude가 처음 "오늘 수첩에 적은 사람은 채연 한 명뿐"으로 풀이한 것은 답변을 넘어선 추측이었다(사용자 지적으로 정정).
- 조치: 반영 (2026-09-18) — 문제 1·2·사례 1·3은 오전 커밋 `cf5a0b7`(수첩 내용 정의·숫자/자기 행동 부정 검사·손목띠 번호 고정). 사례 2(설명 규칙이 장면 행동 반복): 원인은 설명 대사가 시나리오 정적 문장이라 준의 시작 기억을 참조하지 않던 것 — 문장은 `cf5a0b7`에서 이미 "숫자 밑에 작은 글자…"로 수정, 이번에 재발 방지 가드 `explanation_grounding.explanation_line`(설명이 장면 밖 새 낱말 2개 미만이면 연결 지식 문장을 덧붙임)과 준 행동의 `explanation_knowledge` 링크 추가. HANDOFF 10번(준 설명 ↔ '가진 것을 보여준다' 단서 중복)은 디렉터 판단 대기.

### F17 — 밤 "근거 고르기"가 불편하고, 실제 역할(채점 후보 추가)이 이름과 다름 (2026-09-17)

- 장면: 밤 — 추리문 제출 화면 "관찰 기록에서 근거 고르기"
- 요청:
  > 게임 시스템상 추리문 제출할때 근거를 꼭 골라야 하는 이유가 있어? 그냥 근거를 보면서 추리문만 쓰면 안 되는 거야? 근거 고르기가 너무 불편해
- 확인(코드):
  - 필수 아님 — 제출 버튼 조건이 `tapped.size > 0 || freeText.trim().length > 0`(`frontend/components/screens/NightScreen.tsx` 118행). 테스터8은 5회차 모두 근거 0개로 제출했다.
  - 실제 역할 — `night_interactor.draft()`가 고른 기록의 원문을 `source_claims(free_text, tapped_texts)`로 추리문 문장과 합쳐 **채점 후보(내 주장)** 로 넣는다(`backend/apps/engine/app/use_cases/night_interactor.py` 30-35·100-105행). 추리문에 쓰지 않은 내용도 고른 관찰 원문이 진실 명제에 매칭되면 인정될 수 있다.
  - 감점 없음 — 매칭되지 않은 후보는 `wrong_claim_count`로 점수 화면에 "N개의 주장은 세계와 닿지 않았다"만 표시되고 점수에는 영향이 없다(`scoring_rules.cell_score`는 확정 판정만 센다).
  - 그 밖의 쓰임 — 다음 밤으로 선택을 이어 주는 복원(`inherited_note_ids`)뿐.
  - 사례 — 이 판 1회차에서 motive-2(이송 규정)의 매칭 문장이 고른 근거의 방송 조각 "오후 검진을 시작합니다."로 기록됐다(F11). 1회차 근거 선택은 Claude 추천이었다.
- 해석: UI는 "근거"(추리문을 뒷받침하는 인용)라고 부르지만 시스템은 "관찰 원문을 주장으로 덧붙이기"로 쓴다. 조작이 불편한데 역할이 모호하고, 관찰을 많이 고를수록 추리 없이 점수가 오를 수 있는 구조다.
- 오인정 사례(F11 재정정): 2일째 cause-5가 이어받은 근거 원문 조각 "오후 검진을 시작합니다."로 인정됐다가, 3일째 주장 목록에서 근거 조각이 빠지자 사라져 총점 64.2→55.4. 근거 원문이 추리와 무관하게 점수를 만들 수 있다는 직접 증거.
- 개선안:
  - 안 A — 근거 고르기 제거, 추리문만 채점. 조작 단순화·관찰 나열 점수 차단. 근거에 의존하던 플레이어의 점수 하락 가능.
  - 안 B — 선택 기능 유지하되 채점 후보에서 빼고, 추리문 작성 중 옆에 띄워 보는 참고 목록으로만 쓴다.
- 관련: F11(난이도 조정·채점 엄격도 — 근거 원문이 점수에 기여하던 몫이 빠지면 체감 난이도 상승), F7(단서 기록 로그 형식 — 참고 목록 표시 방식), 테스터6 F3·F6(근거 목록 길이·중복).
- 조치: 반영 (2026-09-17 밤, 사용자 결정 **안 B**, 백엔드 커밋 `cf5a0b7`) — 고른 기록을 채점 후보에서 뺌(`source_claims`는 직접 쓴 글만), 노트 패널은 "기록 보며 쓰기" 참고 목록으로, 빈 글은 주장 0개. 계획·리뷰 기록 `docs/superpowers/plans/2026-09-17-night-evidence-reference.md`. 서버 반영 2026-09-18 07:24 재기동. 이 판의 cause-5 오인정(F11 재정정)은 이 변경으로 재발하지 않는다.

### F19 — 신의 질문에서 답을 얻기 어려움("알 수 없다" 편중) (2026-09-17)

- 장면: 밤 — 신의 개입 질문
- 요청:
  > 검진 때 침상 사이를 지나가는 흰 옷 입은 사람들은 누구인가? 라고 물으면 또 알 수 없다 이따구로 답하지 않을까? ~~흰 옷 사람들은 의사인가? 이렇게 물어보면 어때

  > 신의개입에서 답을 얻기 어려운 문제는 어떻게 해결할 수 있어?
- DB 확인(`intervention_question` 이벤트, 사람 표본 4판):

  | 판 | 질문 수 | unknown | supported |
  |---|---:|---:|---:|
  | 테스터6 `2338ec9f` | 12 | 12 | 0 |
  | 테스터7 `61acc8ef` | 12 | 7 | 5 |
  | 테스터8 `7cf7484d` | 2 (5회차 전체) | 2 | 0 |
  | 테스터9 `e6290e17` (2회차 밤 진행 중) | 5 | 3 | 2 (1건은 F10 오판정) |

  31문항 중 24개(77%)가 "알 수 없다". 테스터8은 질문 기능을 거의 쓰지 않았다.
- 원인: 조언자는 플레이어에게 공개된 기록·기본 세계 설명 안에서만 답한다(프롬프트 "숨겨진 정체·원인·결말은 외부 지식으로 보충하지 마라"). 플레이어가 정말 궁금한 것은 기록에 없는 것이라, 필요한 질문일수록 "알 수 없다"가 된다.
- 질문 형태 메모: "흰 옷 사람들은 누구인가?"(찾기형)와 "흰 옷 사람들은 의사인가?"(예/아니오) 모두 판정 재료(정체를 적은 공개 기록)가 없어 결과가 같다. 예/아니오형은 F10 경로로 관찰이 `supported` 표시되면 확인되지 않은 정체에 "맞다"가 붙어 추리를 오도할 위험이 추가된다.
- 해결 방향 (효과 대비 부담이 작은 순):
  1. "알 수 없다"를 헛걸음으로 만들지 않기 — unknown이면 질문 횟수 환급, 또는 현재 붙는 조언(`next_observation`, "내일 ○○에게 … 물어봐라")을 답의 중심으로 크게 표시. F9와 연결.
  2. 판정 신뢰도 확보 — F10 수정 후 가설 검증 사용법 안내(F16).
  3. 답할 수 있는 질문으로 유도 — 입력 예시·추천 질문을 답할 재료가 있는 대상(새 단서·미질문 방송)에서 생성. F18과 연결.
  4. 회차별 공개 범위 확대 — 시나리오에 회차·점수 단계별로 신이 말해도 되는 사실 사다리를 작성하고 단계가 오르면 한 칸씩 연다. 효과가 가장 크나 시나리오 문장(C등급)+답변 로직(B등급) 동시 작업. F11 난이도 조정의 핵심 수단으로 목표 점수대와 함께 설계.
- 코드 위치: `backend/apps/engine/app/use_cases/intervention_interactor.py`(답변·status·조언 조립), `backend/apps/engine/app/use_cases/advisor_advice.py`, 표시 `frontend/components/screens/GodScreen.tsx`.
- 관련: F9·F10·F11·F16·F18, 테스터6 F7(신의 질문 답변의 맥락 부재).
- 조치: 반영 (2026-09-18) — 1·2·4는 `god-question-improvements`(환급·안내·사다리). 3: 규칙 기반 추천 질문 `question_suggestions.suggest_questions`("오늘 방송에서 관리자는 무엇을 말했는가?" / "오늘 소등 뒤에 무엇이 새로 드러났는가?" / "{이름}은 오늘 무엇을 했는가?", 최대 3, 물은 것 제외, 가설·정답 방향 없음)를 `submit`·`questions` 응답 `suggested_questions`로 내고, 신의 개입 화면 입력 아래 "예시:" 칩으로 표시(클릭 시 입력만). "오늘" 질문은 현재 회차 기록 우선.

### F23 — 시나리오 단서 조각이 채팅에 나오지 않고 단서 기록에만 조용히 추가됨 ("귀표"·"충식") (2026-09-17)

- 장면: 3일째 낮 2번째 장면(오전 — 자유 시간) → 6번째 장면에서 발견
- 요청:
  > 준이 "귀표"라는 단어 안 썼는데??

  > 충식도 채팅창에는 전혀 나오지 않아. 충식이란 사람이 있는지도 알 수가 없는 수준이야
- 확인(DB·코드):
  - 3일째 2번째 장면에 시나리오 단서 조각 3개가 관찰로 공개됐다(이벤트 12737~12739): "충식은 어제 이송됐다." · `준: "이 숫자 뭔지 알아?"` · `"귀표"라는 단어를 준이 쓴다.` 정의는 `backend/apps/scenarios/scenario_a/adapter.py` `fragments`(544-547행), 공개는 `backend/apps/engine/app/use_cases/loop_interactor.py` 610-621행(`disclose` → `notes.upsert(kind="fragment")`).
  - 세 줄은 `notes`(단서 기록)에는 들어갔지만 채팅 로그에는 나오지 않았다. 낮 화면은 장면 응답의 `narration`·`ambient` 대사만 채팅에 넣고(`DayScreen.tsx` 323·328행), 관찰 목록(`res.observations`)은 단서 기록 갱신에만 쓴다.
  - 같은 장면 준의 실제 대사는 "민석아, 이 숫자 뭔지 알아?"뿐이고 "귀표"는 없다. 기록은 "준이 귀표라는 단어를 쓴다"인데 화면에서는 그런 장면이 없어 기록과 화면이 어긋난다.
  - "충식"은 인물 설정(`adapter.py` 202-203행, "어제 이송됐다. 이름만 언급된다.")과 NPC 프롬프트("충식은 지금 여기 없다 — 어제까지 있던 이름")에만 있고, 플레이어 화면에는 이 조각 전까지 한 번도 등장하지 않는다.
- 파생 사례: 보조 플레이에서 Claude가 DB 조각을 화면 대사로 오인해 `아까 말한 "귀표"는 무슨 뜻이야?`를 추천했고 그대로 입력됐다(이벤트 12913). 준은 "나도 잘 모르겠어. 숫자 밑에 작은 글자로 써 있었는데, 무슨 뜻인지 아직 몰라."로 답했다. 정정(4일째 확인): "숫자 밑에 작은 글자"는 준의 시작 기억(`jun-before-start-band`, "손목띠 숫자 밑에 작은 글자가 하나 더 있는 것을 봤다")에 있는 내용이라 유도로 지어낸 답이 아니다. 다만 "귀표"라는 말을 자기가 했다는 전제는 반박하지 않았다.
- 해석: 핵심 단서(정체·이송 흐름)가 플레이어가 보는 흐름 밖에서 기록에만 쌓여, 기록을 뒤져 보지 않으면 존재 자체를 모른다. 단서 조각은 화면 연출(대사·장면 설명·NEW 표시)로 드러나야 하고, 인물 행동으로 서술된 조각("준이 쓴다")은 실제 대사와 일치해야 한다.
- 개선 방향: ① 조각을 해당 장면의 채팅/대사창에 표시(인물 조각은 그 인물 대사로, 서술 조각은 장면 설명으로) ② 인물 조각이 있는 장면은 그 인물의 ambient 대사에 해당 단어가 실제로 나오게 맞춤 ③ 이름만 언급되는 인물(충식)은 첫 등장 시 누구인지 알 수 있는 최소 맥락 제공 ④ 새 조각에 NEW 표시(F18).
- 관련: F13(장면 설명 묶음)·F18(NEW 단서)·F20(덩어리 텍스트)·F11(피암시성), 테스터6 F8(단서의 무맥락).
- 조치: 반영 (2026-09-18) — ①·② 낮 화면 재구성(파편을 장면 대사창에 표시, 귀표 대사화). ③ 충식 첫 등장: `같은 방에 있던 충식은 어제 이송됐다. 아침에 그 자리는 깨끗하게 비어 있었다.`(초안, 디렉터 확인 전). ④ NEW 표시·새 단서만 필터(F18). 추가 사례 "7시 13분."은 5회차 1장면 대사창에 서술문으로 보임.
- 추가 사례(5일째 1번째 장면): 5일째 전용 조각 "7시 13분."(`adapter.py` `FragmentDTO(loop_n=5, …)`)도 단서 기록에만 들어가고, 화면에는 매일 같은 장면 제목 "기상 — 7:12"만 보인다. 제목과 1분 어긋나게 심은 단서로 보이나 플레이어는 어긋남을 알아챌 수 없다. 보조 플레이 중 Claude가 이 조각을 화면 정보처럼 전달했다가 사용자 지적으로 정정.

### F24 — 등장인물이 글자를 읽지 못한다는 설정과 달리 트럭 표기 단서가 읽힌 문자열로 제시됨 (2026-09-17)

- 장면: 4일째 밤 결말 단서(트럭 결말) → 5일째 추리문 작성 중
- 요청 (스포일러 방지를 위해 정체 단어는 `[정체]`로 치환):
  > 근데 주인공도 [정체]니까 "[트럭 표기]"라는 글자를 읽을 수 없을 텐데 명시되는 게 이상해. 이 점도 수정해야될거 같아.
- 확인(코드):
  - 4일째 트럭 결말 조각은 트럭 옆면 표기를 **읽힌 문자열 그대로** 보여준다(`backend/apps/scenarios/scenario_a/adapter.py` 549행 `FragmentDTO(loop_n=4, beat=6, world_outcome="truck", …)`).
  - 같은 시나리오의 다른 문자 단서는 모두 "읽지 못함"으로 설정돼 있다 — 준의 시작 기억 "배급 포대 옆면에 글자가 있다. 읽을 줄 몰라서 무슨 뜻인지는 모른다."(189행), R6 포대 장면 "큰 글자가 찍혀 있는데 아무도 읽지 못한다."(302행), 4일째 밤 단서 캡션 "배급 포대에 글자가 있다."(566행)·방송 "포대의 표기는 관리 용도입니다. 읽을 필요 없습니다."(565행).
- 해석: 시점 인물(플레이어가 보는 세계의 인물들)은 글자를 읽지 못하는데 트럭 표기만 읽혀 나오는 것은 설정 모순이다. 동시에 이 표기는 정체 추리의 결정적 단서라, 읽힌 문자열을 빼면 난이도가 더 오른다(F11).
- 개선 방향(시나리오 문장, C등급): ① 읽을 수 없는 "모양"으로 제시(예: 글자 모양·그림 표시·로고 형태 묘사)하고 뜻은 다른 단서와 대조로 추리하게 함 ② 글자를 읽을 수 있는 존재(관리자 방송·담당자 대사 등)의 입을 통해 간접 노출 ③ 문자열을 유지하려면 "읽을 수 있다"는 예외 설정을 시나리오에 명시. 어느 쪽이든 조언자가 이 단서를 해석하는 방식(4일째 밤 "○○…은 무엇을 하는 곳인가" 답)과 채점 명제의 인정 범위를 함께 점검.
- 관련: F11(난이도 — 결정 단서 약화 시 영향), F23(단서 조각 노출 방식), 테스터6 F11.
- 조치: 반영 (2026-09-18, ② 방식, HANDOFF 2번) — 4회차 트럭 결말 조각 `트럭 옆면 "○○축산".` 삭제. 5회차 낮 마지막 장면 끝에 관리자 방송 MA13 "축산 차량이 출발합니다. 문이 닫힐 때까지 자리에서 움직이지 마십시오."(번들 `day_end_broadcasts`, 음성 `MA13.mp3` 등록, P06 트럭 그림은 그 줄이 현재 줄일 때만 표시·캡션 "트럭 옆면에도 글자가 있다."). NPC 기억에는 넣지 않아 '축산' 발화 금지 유지, 신의 질문 근거 검색에 축산 주제 추가. 시나리오 디렉터 확인 전(`초안`).

### F26 — 판 API에 소유자 확인이 없음 (보안, A등급) (2026-09-17)

- 계기: F25(개발 데이터에 회차 uuid 노출) 기록 중 확인
- 요청:
  > A등급은 지금 당장 점검해야 하는 것들인거야?

  > 지금 다른 팀원이 플레이하고 있어서 고치지는 말고 피드백에 넣어만 줘
- 확인(코드, `backend/apps/engine/adapter/inbound/api/v1/game_router.py`):
  - 상태를 바꾸는 판 API(발화·장면 진행·원숭이손 응답·추리문 초안/수정/제출·신의 질문·규칙 선택/미리보기)는 로그인(`require_user`)만 요구하고, 로그인 사용자가 그 판의 주인인지는 확인하지 않는다(`user`를 인터랙터에 넘기지 않음).
  - 판 내용을 읽는 조회 API(단서 기록·관찰·이전 추리문·인물 상태·규칙 선택지·회고·개발 데이터)는 로그인도 요구하지 않는다. 규칙 선택지 조회는 선택지 생성(모델 호출)을 일으킬 수 있다.
- 영향: 판·회차·밤 ID를 아는 사람은 남의 판을 보거나(비로그인), 로그인 후 조작할 수 있고 그 과정에서 모델 호출 비용이 발생한다.
- 위험도 판단: ID가 무작위 uuid라 추측으로 찾기는 어렵다. 다만 공개 주소(api.remakeday.com)가 열려 있고, 화면(F25)·스크린샷·브라우저 개발자 도구로 ID가 새어 나갈 수 있어 09-20 제출 전 수정 대상이다.
- 수정 방향: 모든 판 API에서 "요청한 로그인 사용자 = 판 주인"을 확인하고, 조회 API에도 로그인을 요구한다. 개발 계정·`GUARD_AUTH=off` 경로 동작과 인스펙터 토큰 경로는 따로 확인한다.
- 절차: A등급(인증·비용) — task마다 구현+검토(sonnet), 최종 전체 리뷰(opus), 수정 1회 + 범위 재검토. 서버 재시작이 필요하므로 진행 중인 플레이가 없을 때 반영.
- 공개 주의: 수정 전에는 이 항목의 세부 내용을 공개 저장소 커밋·데브로그 공개 포스트에 싣지 않는다.
- 조치: 반영 (2026-09-17 밤, 백엔드 커밋 `7141943`) — 판 하위 라우트 17개 전부 로그인 + 판 주인 확인(`guards.require_attempt_owner`, 소유 판정은 `AttemptRepository.get_owned` 한 곳; 남의 판·없는 판·잘못된 ID는 모두 404로 같게 응답). 후속 강화 F26b: 공개 설정에서 `GUARD_AUTH=off` 기동 거부, API 문서 라우트 비공개, 인스펙터 토큰을 헤더로. 계획·리뷰 기록 `docs/superpowers/plans/2026-09-17-f26-attempt-ownership.md`·`2026-09-17-f26b-hardening.md`. 서버 반영 2026-09-18 07:24 재기동.

기술 점검·빈 세션 판은 이 참가자의 표본으로 사용하지 않는다.