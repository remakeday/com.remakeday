# HANDOFF 2026-09-18 — 아침 팀 결정 목록

09-17 휴먼테스트(테스터8~11) 반영 배치에서 남은 결정. 서브에이전트 작업이 끝나면 이 문서를 최신화한다. 기획서 정본은 `docs/define/REMAKE_DAY_기획서_확정본_v9.0_2026-09-10.md`.

## 진행 순서 (확정)

1. 진행 중 작업 마무리 — 5b 비용 동시성(A), DB 비밀번호 절차 문서 리뷰 반영
2. **12번 검증** — DB 비밀번호 교체 → 백엔드·프론트 재시작 → 백엔드 전체 pytest 단독 → 헤드리스 전체(한 번에 하나) → 실서버 스모크
3. 12번 통과 뒤 **작업별로 나눠 커밋**
4. (아침, 팀 상의 뒤) 10번 낮 화면 재구성 → 9b 트럭 방송 연결

## 아침에 팀이 정할 것

### 화면·연출
| # | 결정 | 근거·자료 |
|---|---|---|
| 1 | 낮 화면 재구성 설계 승인 (인물 초상 중심·대사창·HUD·기록 메뉴, 서버 `lines`) | `docs/superpowers/specs/2026-09-17-day-screen-vn-design.md` |
| 2 | 9b 트럭 방송 연결 — 5회차 낮 끝 MA13(녹음 완료)·P06(제작 완료), 4회차 "○○축산" 조각 삭제 | 이미지제작서 원숭이손Q군 v2 P06 절, `docs/voice.md` §10 |
| 3 | 소리 — 랜딩 BGM(기존 곡 재사용 여부, 버튼 등장 4.2초·우상단 토글), 장면 전환 대기 효과음(freesound 라이선스 CC0/CC BY만) | 테스터9 F3·F15 |
| 4 | 관리자 방송 팝업 확인 버튼이 늘 "…"로만 보임 — 라벨 바꿀지 | 프론트 묶음1 발견 |
| 5 | 첫 화면(EntryScreen) 요약 3줄에 게임 목표 한 줄 추가 여부 | 7번 안내 묶음 |

### 시나리오 문장 (시나리오 디렉터 확인 — 코드에 `초안` 주석)
| # | 대상 | 비고 |
|---|---|---|
| 6 | 원숭이손 소원 4종 문장·반대 사건 서술·팝업 대가 암시 "대가 없이 굴러오는 것은 없다." | 설계 문서 §3, v9.0 §8 |
| 7 | 신의 질문 안내 답 2개·입력 안내 2줄·공개 사다리 8칸 | 3·4단계 정답 근접은 유지로 결정됨 |
| 8 | 손목띠 번호 — 채연14·준23·은상25·충식32·민석41 | 사용자 결정: 그대로 |
| 9 | 수첩 서술 "수첩에 뭔가 적는다"·"아무도 채연의 쟁반을 적지 않았다" 등 "적다"를 "표식을 남긴다"로 바꿀지 | 메타포 원칙(v9.0 §4). 바꾸면 테스트 4개·v9.0 §8 예문 수정 |
| 10 | 준 설명("숫자 밑에 작은 글자")과 규칙 행동 "가진 것을 보여준다"의 단서 중복 | 9번 NPC 품질 |
| 11 | 민석 방송실 기억에 "무엇을 알렸는지" 넣을지 (넣으면 보고 질문 규칙 우회) | 9번 NPC 품질 |
| 12 | 문구: EntryScreen "손목띠 숫자를 자꾸 들여다본다", GodScreen "손목의 번호는 이름보다 먼저 있었다." — 메타포 원칙에 맞출지 | |
| 13 | 원숭이손 삽화 다듬기(선택) — Q02 v2 유령 느낌, Q10 이중 윤곽, P06 차량 느낌 | 검수는 전부 통과 |

### 문서·제출 준비
| # | 항목 |
|---|---|
| 14 | v9.0 남은 불일치 — §10.3 쿠키 규칙의 부작용 칸, §7.2 선택지(조언 규칙 맨 앞), §11.6 모델 표(제출 조합), 원숭이손 설계 문서 앞부분 "v8.1 정본" 문구 |
| 15 | 제출 전환 체크리스트 — `TRUST_PROXY=true`(터널 뒤 IP 버킷), **`.env`의 `USER_DAILY_ATTEMPTS`(기본 5)·`DAILY_ATTEMPT_CAP`(기본 200) 기본값 복귀**(지금 테스트용으로 크게 올라가 있어 한 사용자 비용 상한이 사실상 없음), Core·NPC 모델 조합 전환 |
| 15-1 | Anthropic 키 넣기 전 비용 후속(5b opus 리뷰) — 발화 모델 실패가 롤백돼 무료로 반복 가능(분당 30 버킷만 상한), 다음 장면·제출·회차 시작에 사용자 단위 버킷 없음, 동시에 열 수 있는 판 수 제한 없음, 채점 모델 실패 시 0점 커밋·재채점 불가 — 어디까지 할지 결정 |
| 16 | 모델 선정 러너 `run_core_selection.py`가 `/health`의 모델 이름을 읽는 부분 → 러너 전용 경로로(작은 수정) |

## 09-17~18 완료 요약 (미커밋)

원숭이손 소원 재설계·구현, 프론트 묶음1(화면 밀림 등), 판 API 소유자 확인(F26)·보안 후속(F26b), 밤 기록 참고 목록·질문형 표시, 밤 점수 확인 신호, 백엔드 버그 묶음(관리자 기억 수정·화자 혼동·조언 규칙·규칙 거부 이유)·계획 수정 제거, 안내 묶음, 신의 질문 개선(환급·안내 답·사다리·동시성 잠금), NPC 답변 품질(수첩·숫자·자기 행동 부정 검사), `/health` 최소화, 민석 수첩·손목띠 메타포, 기획서 정본 v9.0 이식, DB 비밀번호 코드 제거. 작업별 계획·리뷰 기록은 `docs/superpowers/plans/2026-09-1{7,8}-*.md`.

## 09-18 저녁 — 결정·완료 (최종 제출을 오늘로 당김)

| # | 결과 |
|---|---|
| 1·2 | 낮 화면 재구성·9b 트럭 방송 반영(오후). 저녁에 팀원이 대사창을 누적 채팅+타이핑으로 재설계, 장면 그림은 타이핑 동안만(사용자 확인) |
| 3 | 랜딩 BGM 반영. 효과음(F15)은 제외 |
| 4·5 | 남은 "…" 2곳 문구로. EntryScreen 목표 줄은 안 넣음(안내 첫 줄이 이미 원인·동기를 말함) |
| 6·7·8·13 | 원숭이손 창작 문구 6곳 그대로 확정. 손목띠 번호 그대로. 사다리 유지. 삽화 다듬기 안 함 |
| 9·11·12 | 미결 — 제출에 영향 없음, 제출 후 과제 |
| 10 | 준 설명·기억을 "뭔가 더 있다"까지로, "작은 글자"는 규칙 장면에서만(`9372340`) |
| 14 | 실제 불일치 2건 정정(`094e083`), 2건은 이미 일치 |
| 15 | **맨 마지막 한 번에**: `USER_DAILY_ATTEMPTS=5`·`DAILY_ATTEMPT_CAP=200`·`TRUST_PROXY=true`·Core·NPC Anthropic. 사람 1판·평가 러너 뒤 |
| 15-1 | 한도 복귀로만. 나머지 구멍은 제출 후 과제 |
| 16 | 완료(`1fa49e6`) |
| F11 | 목표 점수대는 사람 1판 재측정 뒤 결정 |
| main | ff 머지 완료, Vercel 프로덕션 브랜치 = main |

남은 순서: 프론트 커밋·푸시 → 백엔드 재기동 → **사람 1판** → Anthropic 키 → 15번 전환 → 스모크 → 최종 제출 선언.

### 테스터12(19:06~) 반영 — 사용자 본인 재측정
점수 8.8→8.8→27.5×3, 3회차 이후 질문 미사용. F1 음성 보류·F3 푸터·F2 쟁반·F6 합성 파편·F5 3회 고정+점진 공개 반영. F4 안내 문구·F9 그래프 Codex. F7 전반 어색은 제출 후. F11: F5 뒤 점수 보고 판단(오르면 유지).

## 최종 배포 절차 — 팀장 류준의 "최종 배포 준비" 신호 뒤 (2026-09-18 21:00 확정)

**진행 상태 (2026-09-18 23:15, 신호 수신 22:54 뒤 컨트롤러 실행):**

| 단계 | 상태 |
|---|---|
| 1 프론트 마감 | tsc 통과, 헤드리스 11종 통과(`npc-followup`은 타이핑 연출 뒤 입력 해제를 기다리도록 테스트 1줄 수정 — 제품 동작은 정상), `feat/coherence-chain` 푸시 완료. **`main` ff 푸시와 Vercel 확인은 미완** — 컨트롤러의 프로덕션 배포 권한이 막혀 사용자 실행 필요(`git push origin feat/coherence-chain:main`, 14+1커밋 ff) |
| 2 키 교체 | 사용자가 Anthropic 5줄 삽입, 컨트롤러가 ollama 중복 4줄 제거 |
| 4·5 재기동·스모크 | Anthropic 구성으로 재기동 → 로컬·터널 health 200 → 개발 로그인 스모크(판 생성·1회차 11.9s·다음 장면 8.6s·NPC 답변 3.5s 전부 200). DB harness 기록: planner·manager_check·manager_paw·classifier = claude-sonnet-5, agent = claude-haiku-4-5, 폴백 0 |
| 3 전환 1단계 | 하루 3판·전체 200·프록시 신뢰 적용, 재기동. 오늘 72판을 만든 개발 계정의 판 생성이 `daily_attempt_limit` 403으로 거부됨 — **순서를 스모크 뒤로 바꾼 이유**: 개발 계정이 오늘 이미 3판을 넘어 한도를 먼저 낮추면 스모크가 자정까지 막힌다 |
| 5-1 전환 2단계 | 개발 로그인 끔, 재기동. 공개 주소 확인: 개발 로그인 404 · 구글 로그인 시작 307 · 비로그인 판 생성 401 · API 문서 404 |
| 6 최종 선언 | 사용자 대기 (`main` 푸시·Vercel success 확인 뒤) |

주의: 오늘(KST) 전체 판 수는 76/200이고 자정에 초기화된다. 사용자 구글 계정은 오늘 3판을 이미 써 자정 전에는 새 판을 만들 수 없다.

전제: 팀원 프론트 커밋 완료, 새 Anthropic 키 준비. 순서대로, 각 단계 통과 확인 뒤 다음.

**현재 서버 상태(21:30):** 8500은 ollama(gemma4/kanana/bge)로 도는 중. 사용자가 `.env`에서 Anthropic 5줄(키·provider·model)을 지워 **`.env`도 ollama 구성** — 재기동해도 로컬로 뜬다. 류준 신호 뒤 2번에서 새 키로 5줄을 다시 넣는다.

1. **프론트 마감(컨트롤러)** — 팀원 커밋 확인 → EntryScreen 준 소개 "손목띠 숫자를" → "손목띠를"(HANDOFF 12번) → **이탈 경고**: 판이 진행 중(진입 화면 이후~회고 전)일 때 새로고침·탭 닫기·뒤로가기에 브라우저 확인창(`beforeunload`, "나가면 이 판은 사라집니다" — 브라우저가 문구를 바꿔 보여줄 수 있음) → `tsc` → 헤드리스 10종(한 번에 하나) → 커밋 → `feat/coherence-chain` 푸시 → `main` ff 푸시 → Vercel 배포 success 확인(GitHub commit status).
2. **키 교체(사용자/류준)** — `backend/.env`에서 ollama `CORE_LLM_PROVIDER`·`CORE_LLM_MODEL`·`NPC_LLM_PROVIDER`·`NPC_LLM_MODEL` 4줄을 지우고 아래 5줄을 넣는다(중복 금지). `*_LLM_THINK`·`EMBEDDING_PROVIDER=gemini`는 그대로.
   ```
   ANTHROPIC_API_KEY=<새 키>
   CORE_LLM_PROVIDER=anthropic
   CORE_LLM_MODEL=claude-sonnet-5
   NPC_LLM_PROVIDER=anthropic
   NPC_LLM_MODEL=claude-haiku-4-5
   ```
3. **제출 전환 1단계(컨트롤러)** — `.env`에서 `USER_DAILY_ATTEMPTS=3`(**한 계정 하루 3판** — 사용자 결정 21:15: 이탈 경고가 있으면 3판이면 충분, 처음 하는 사람 3판 ≈ 1시간. `count_today`가 오늘 만든 판 수를 세므로 중도 포기 포함, 자정 초기화) · `DAILY_ATTEMPT_CAP=200` · `TRUST_PROXY=true`. `DEV_LOGIN`은 아직 `on`(스모크용). 값은 문서에 적지 않는다. 사용자 지시 2026-09-18 21:05.
4. **재기동** — `fuser -k 8500/tcp`로 끊고(패턴 pkill 금지 — 같은 줄의 시작 명령을 자기 자신으로 잡아 셸이 죽는다) 절대경로로 uvicorn 시작 → `/health` 200(로컬·터널) → `get_settings()`로 `anthropic claude-sonnet-5 | anthropic claude-haiku-4-5 | key set: True` 확인.
5. **실서버 스모크(개발 로그인으로)** — 개발 로그인 → 판 생성 → 1회차 진입 → 다음 장면 1회(관리자 검사·계획 = Sonnet 호출) → NPC에게 질문 1회(Haiku 호출) → 응답 200·오류 0. **새 키가 실제로 통하는지** 여기서 확인. 4번째 판 생성이 거부되는지(하루 3판)도 같은 계정으로 확인.
5-1. **제출 전환 2단계 — 구글 로그인만** — `.env`에서 `DEV_LOGIN=off` → 재기동 → `POST /api/v1/auth/dev-login`이 거부(401/403)되고 `GET /api/v1/auth/google/start`가 307인지, 비로그인 `POST /sessions`가 401인지 확인. 프로덕션 프론트엔 개발 로그인 폼이 애초에 없다(`NEXT_PUBLIC_DEV_LOGIN` 미설정).
6. **최종 선언(사용자)** — "최종 제출 완료". 이후 코드 동결.

롤백(어느 단계든): provider 두 줄을 `ollama`, 모델을 `gemma4:12b`/`kanana1.5:8b-q4km`로, 한도는 테스트값으로 되돌리고 재기동.
